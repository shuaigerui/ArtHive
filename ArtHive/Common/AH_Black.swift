
import Foundation

import UIKit

import StoreKit

enum AS_IAPError: LocalizedError, Equatable {
    case productNotFound
    case purchaseCancelled
    case purchasePending
    case verificationFailed
    case unknown

    var errorDescription: String? {
       var interfaces: [String: Any]! = [String(cString: [97,118,111,112,116,105,111,110,115,0], encoding: .utf8)!:String(cString: [115,105,122,101,108,101,115,115,0], encoding: .utf8)!, String(cString: [116,95,52,57,0], encoding: .utf8)!:String(cString: [112,115,115,104,0], encoding: .utf8)!, String(cString: [97,112,97,114,97,109,115,0], encoding: .utf8)!:String(cString: [108,117,104,110,0], encoding: .utf8)!]
       var privacy4: Double = 2.0
       var backgroundZ: [Any]! = [21, 100]
       var updatedc: Double = 0.0
      withUnsafeMutablePointer(to: &updatedc) { pointer in
             _ = pointer.pointee
      }
         backgroundZ = [(Int(updatedc > 307990813.0 || updatedc < -307990813.0 ? 80.0 : updatedc) << (Swift.min(4, labs(1))))]
         privacy4 /= Swift.max((Double(Int(updatedc > 97701536.0 || updatedc < -97701536.0 ? 38.0 : updatedc))), 2)
          var fansO: [Any]! = [76, 16]
          _ = fansO
          var captureo: Double = 1.0
          var category_wB: String! = String(cString: [99,111,110,116,111,117,114,115,0], encoding: .utf8)!
         updatedc -= Double(fansO.count | 1)
         captureo += (Double(backgroundZ.count << (Swift.min(4, labs(Int(updatedc > 100514912.0 || updatedc < -100514912.0 ? 59.0 : updatedc))))))
         category_wB.append("\(backgroundZ.count)")
      for _ in 0 ..< 2 {
          var bottomB: Bool = true
          var modityC: Float = 4.0
          _ = modityC
         updatedc /= Swift.max(5, (Double(3 >> (Swift.min(labs(Int(modityC > 50663422.0 || modityC < -50663422.0 ? 46.0 : modityC)), 2)))))
         bottomB = privacy4 == Double(modityC)
      }
      if (5.37 * updatedc) <= 1.47 || (updatedc * privacy4) <= 5.37 {
         updatedc -= (Double(Int(privacy4 > 180400376.0 || privacy4 < -180400376.0 ? 36.0 : privacy4) ^ 1))
      }
          var covert: String! = String(cString: [105,109,112,108,105,101,115,0], encoding: .utf8)!
          var controllerk: [Any]! = [42, 3]
          _ = controllerk
          var permissionsM: String! = String(cString: [114,111,119,107,101,121,0], encoding: .utf8)!
         backgroundZ = [3]
         covert.append("\(1)")
         controllerk.append(covert.count * 3)
         permissionsM = "\(2 & permissionsM.count)"
         backgroundZ.append((Int(privacy4 > 203206813.0 || privacy4 < -203206813.0 ? 23.0 : privacy4) ^ 2))
      for _ in 0 ..< 1 {
          var anewsr: String! = String(cString: [108,97,115,114,0], encoding: .utf8)!
         privacy4 /= Swift.max(Double(anewsr.count / 1), 2)
      }
         privacy4 *= (Double(backgroundZ.count * Int(privacy4 > 240194231.0 || privacy4 < -240194231.0 ? 21.0 : privacy4)))
      interfaces["\(updatedc)"] = (Int(updatedc > 256279025.0 || updatedc < -256279025.0 ? 72.0 : updatedc))

        switch self {
        case .productNotFound:
            return "Product not found"
        case .purchaseCancelled:
            return nil
        case .purchasePending:
            return "Purchase is pending approval"
        case .verificationFailed:
            return "Purchase verification failed"
        case .unknown:
            return "Purchase failed"
        }
    }
}

final class AH_Black {
private var colorSculptureNetwork_str: String!
private var questionsSize: Double? = 0.0



    static let shared = AH_Black()

    struct AH_EditHistoryItem {
var likeMax: Float? = 0
var followingContactClearString: String?
var price_padding: Double? = 0



        let productId: String
        let coins: Int
        let fallbackPrice: String
    }

    static let catalog: [AH_EditHistoryItem] = [
        AH_EditHistoryItem(productId: "wtcidpxiocqnppk", coins: 63700, fallbackPrice: "$99.99"),
        AH_EditHistoryItem(productId: "pgtieveltsycdcen", coins: 29400, fallbackPrice: "$49.99"),
        AH_EditHistoryItem(productId: "tzgfqstbqimybiqe", coins: 10800, fallbackPrice: "$19.99"),
        AH_EditHistoryItem(productId: "jghmriplhfvgtsmb", coins: 5150, fallbackPrice: "$9.99"),
        AH_EditHistoryItem(productId: "elkscjqwadgvgwwn", coins: 2450, fallbackPrice: "$4.99"),
        AH_EditHistoryItem(productId: "wbovxlluaqphahot", coins: 800, fallbackPrice: "$1.99"),
        AH_EditHistoryItem(productId: "kuuinchfowwrdptw", coins: 400, fallbackPrice: "$0.99")
    ]

    private var storeProducts: [String: Product] = [:]
    private var updatesTask: Task<Void, Never>?

    private init() {
        updatesTask = Task { [weak self] in
            await self?.listenForTransactions()
        }
    }

    var shopItems: [AH_CurrentItem] {
        Self.catalog.map { item in
       var targetY: String! = String(cString: [115,109,111,111,116,104,101,110,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &targetY) { pointer in
          _ = pointer.pointee
   }
       var requestp: Double = 5.0
       _ = requestp
       var controllerz: String! = String(cString: [105,110,111,117,116,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &controllerz) { pointer in
             _ = pointer.pointee
      }
       var all4: String! = String(cString: [103,108,111,98,97,108,115,0], encoding: .utf8)!
      while ((requestp / (Swift.max(3.69, 4))) > 5.25) {
         controllerz = "\(all4.count | 2)"
         break
      }
      while (controllerz == all4) {
         all4.append("\(all4.count)")
         break
      }
          var refreshn: [String: Any]! = [String(cString: [100,101,99,111,109,112,114,101,115,115,105,111,110,0], encoding: .utf8)!:String(cString: [116,114,97,110,115,105,116,105,111,110,101,100,0], encoding: .utf8)!, String(cString: [109,105,100,105,0], encoding: .utf8)!:String(cString: [110,97,118,105,103,97,116,111,114,0], encoding: .utf8)!]
          var cachedh: Double = 3.0
          var froml: Bool = true
         controllerz = "\(2)"
         refreshn["\(cachedh)"] = (2 % (Swift.max(5, Int(cachedh > 343890448.0 || cachedh < -343890448.0 ? 35.0 : cachedh))))
         froml = controllerz.count <= 61
       var introb: String! = String(cString: [99,104,97,99,104,97,0], encoding: .utf8)!
      while (introb.count >= all4.count) {
          var logged5: String! = String(cString: [108,101,109,111,110,0], encoding: .utf8)!
         all4.append("\(introb.count / (Swift.max(logged5.count, 8)))")
         break
      }
      repeat {
         controllerz.append("\(((String(cString:[98,0], encoding: .utf8)!) == introb ? introb.count : Int(requestp > 288418990.0 || requestp < -288418990.0 ? 49.0 : requestp)))")
         if controllerz == (String(cString:[55,109,122,118,120,0], encoding: .utf8)!) {
            break
         }
      } while (controllerz == (String(cString:[55,109,122,118,120,0], encoding: .utf8)!)) && (all4 == controllerz)
          var mediaR: Double = 1.0
         introb = "\(1 >> (Swift.min(5, introb.count)))"
         mediaR /= Swift.max(5, (Double(all4 == (String(cString:[82,0], encoding: .utf8)!) ? all4.count : introb.count)))
      if (all4.count >> (Swift.min(labs(3), 3))) <= 5 {
          var default_i4: [String: Any]! = [String(cString: [119,111,114,107,101,114,115,0], encoding: .utf8)!:18, String(cString: [102,114,101,101,0], encoding: .utf8)!:11]
          var list5: String! = String(cString: [104,113,97,100,115,112,0], encoding: .utf8)!
          var inputR: [Any]! = [46, 34]
          var merget: String! = String(cString: [115,101,103,119,105,116,0], encoding: .utf8)!
         all4 = "\((1 | Int(requestp > 348107167.0 || requestp < -348107167.0 ? 44.0 : requestp)))"
         default_i4["\(all4)"] = default_i4.values.count
         list5.append("\(2)")
         inputR.append(controllerz.count ^ 3)
         merget = "\(inputR.count | list5.count)"
      }
      if requestp == 1.65 {
         all4.append("\(introb.count - 2)")
      }
      targetY.append("\(controllerz.count + 1)")

            return AH_CurrentItem(
                productId: item.productId,
                coins: item.coins,
                price: storeProducts[item.productId]?.displayPrice ?? item.fallbackPrice
            )
        }
    }

@discardableResult
 func migrateStyleTextGenerator(modelHorizontal: Int) -> [Any]! {
    var requestY: String! = String(cString: [109,101,109,98,101,114,115,0], encoding: .utf8)!
    var emptyG: Int = 0
    var passwordx: [Any]! = [53, 72, 21]
       var requestL: String! = String(cString: [105,110,108,101,110,0], encoding: .utf8)!
      repeat {
         requestL.append("\(requestL.count)")
         if (String(cString:[122,51,56,121,95,101,119,0], encoding: .utf8)!) == requestL {
            break
         }
      } while ((String(cString:[122,51,56,121,95,101,119,0], encoding: .utf8)!) == requestL) && (1 <= requestL.count)
         requestL.append("\(requestL.count)")
      for _ in 0 ..< 3 {
          var greenN: String! = String(cString: [118,105,115,105,116,0], encoding: .utf8)!
          _ = greenN
          var photos8: String! = String(cString: [102,105,114,101,100,0], encoding: .utf8)!
          var checkedF: String! = String(cString: [115,101,114,118,105,99,101,115,0], encoding: .utf8)!
          var horizontalA: String! = String(cString: [115,108,105,99,101,100,0], encoding: .utf8)!
         requestL = "\(checkedF.count)"
         greenN.append("\(2)")
         photos8.append("\(1)")
         horizontalA = "\(photos8.count)"
      }
      requestY = "\(requestY.count * emptyG)"
   repeat {
      passwordx = [3]
      if passwordx.count == 4739701 {
         break
      }
   } while (!passwordx.contains { $0 as? Int == emptyG }) && (passwordx.count == 4739701)
   repeat {
      requestY = "\(1)"
      if requestY.count == 2104932 {
         break
      }
   } while (3 == requestY.count) && (requestY.count == 2104932)
   return passwordx

}





    func loadProducts() async {

         var getrandomClaim: [Any]! = migrateStyleTextGenerator(modelHorizontal:1)

      getrandomClaim.enumerated().forEach({ (index,obj) in
          if index  >  93 {
                          print(obj)
          }
      })
      var getrandomClaim_len = getrandomClaim.count

withUnsafeMutablePointer(to: &getrandomClaim) { pointer in
    
}


       var videoJ: Float = 0.0
      videoJ -= (Float(Int(videoJ > 323865652.0 || videoJ < -323865652.0 ? 15.0 : videoJ) / 1))

        let layout = Set(Self.catalog.map(\.productId))
        guard let products = try? await Product.products(for: layout) else { return }
        for product in products {
            storeProducts[product.id] = product
        }
    }

    func purchase(productId: String) async throws -> Int {
       var config5: Float = 5.0
   repeat {
      config5 -= (Float(Int(config5 > 85714789.0 || config5 < -85714789.0 ? 93.0 : config5) | Int(config5 > 344170460.0 || config5 < -344170460.0 ? 97.0 : config5)))
      if 4464820.0 == config5 {
         break
      }
   } while (4464820.0 == config5) && (2.11 >= (1.93 - config5) || (1.93 / (Swift.max(3, config5))) >= 2.13)

        if storeProducts[productId] == nil {
            await loadProducts()
        }
        guard let product = storeProducts[productId] else {
            throw AS_IAPError.productNotFound
        }

        let result = try await product.purchase()
        switch result {
        case .success(let verification):
            let gradient = try verify(verification)
            defer { Task { await gradient.finish() } }
            guard let coins = Self.catalog.first(where: { $0.productId == productId })?.coins else {
                throw AS_IAPError.unknown
            }
            return coins
        case .userCancelled:
            throw AS_IAPError.purchaseCancelled
        case .pending:
            throw AS_IAPError.purchasePending
        @unknown default:
            throw AS_IAPError.unknown
        }
    }

@discardableResult
 func shouldRangeShop(baseContent: [Any]!, providerStore: [String: Any]!, personClear: [String: Any]!) -> String! {
    var styleQ: Bool = false
    var idsM: Double = 2.0
    var bases: String! = String(cString: [115,117,98,108,97,121,111,117,116,0], encoding: .utf8)!
   if (3 ^ bases.count) == 5 {
       var pressw: String! = String(cString: [105,99,111,110,115,0], encoding: .utf8)!
       _ = pressw
       var rangeQ: Double = 2.0
          var conversationu: [String: Any]! = [String(cString: [109,117,109,98,97,105,0], encoding: .utf8)!:63, String(cString: [100,101,98,97,110,100,0], encoding: .utf8)!:84, String(cString: [97,95,56,49,0], encoding: .utf8)!:67]
         pressw.append("\((pressw == (String(cString:[76,0], encoding: .utf8)!) ? pressw.count : Int(rangeQ > 315364491.0 || rangeQ < -315364491.0 ? 50.0 : rangeQ)))")
         conversationu[pressw] = pressw.count * 2
      while (pressw.hasSuffix("\(rangeQ)")) {
          var coins5: Int = 1
         withUnsafeMutablePointer(to: &coins5) { pointer in
                _ = pointer.pointee
         }
          var clearp: String! = String(cString: [99,121,99,108,101,100,0], encoding: .utf8)!
          var icon3: String! = String(cString: [104,121,98,114,105,100,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &icon3) { pointer in
    
         }
          var capture3: String! = String(cString: [103,101,116,101,110,118,0], encoding: .utf8)!
          var interval_3sS: Float = 2.0
         rangeQ -= Double(icon3.count ^ 1)
         coins5 &= 1 >> (Swift.min(2, clearp.count))
         clearp.append("\(1)")
         capture3.append("\(pressw.count % 2)")
         interval_3sS -= Float(coins5 | 2)
         break
      }
         rangeQ += Double(pressw.count)
      while (4.14 >= rangeQ) {
          var spacingm: String! = String(cString: [122,105,103,122,97,103,0], encoding: .utf8)!
          var horizontalZ: Float = 5.0
          var paintq: Double = 1.0
          _ = paintq
          var actionx: Bool = true
         pressw.append("\((2 & Int(horizontalZ > 233287076.0 || horizontalZ < -233287076.0 ? 15.0 : horizontalZ)))")
         spacingm.append("\(((actionx ? 4 : 5) - 3))")
         paintq /= Swift.max(2, (Double((String(cString:[102,0], encoding: .utf8)!) == spacingm ? spacingm.count : Int(horizontalZ > 88214942.0 || horizontalZ < -88214942.0 ? 66.0 : horizontalZ))))
         actionx = spacingm.count <= 97 && 62.24 <= paintq
         break
      }
         pressw = "\((pressw == (String(cString:[52,0], encoding: .utf8)!) ? pressw.count : Int(rangeQ > 213777428.0 || rangeQ < -213777428.0 ? 68.0 : rangeQ)))"
         rangeQ += (Double(pressw.count + Int(rangeQ > 249076283.0 || rangeQ < -249076283.0 ? 82.0 : rangeQ)))
      bases.append("\((Int(idsM > 282232945.0 || idsM < -282232945.0 ? 46.0 : idsM)))")
   }
       var participantp: Double = 2.0
       _ = participantp
       var register_nV: [Any]! = [94.0]
       var updatedS: String! = String(cString: [118,97,108,105,100,97,116,105,111,110,0], encoding: .utf8)!
         participantp /= Swift.max(1, (Double(updatedS.count + Int(participantp > 181840992.0 || participantp < -181840992.0 ? 33.0 : participantp))))
      if participantp < 2.29 {
         register_nV.append(2 % (Swift.max(7, updatedS.count)))
      }
         updatedS = "\(3)"
          var a_heightp: Double = 2.0
          var resolvedM: Int = 3
         register_nV = [(Int(participantp > 29645755.0 || participantp < -29645755.0 ? 73.0 : participantp))]
         a_heightp += Double(updatedS.count)
         resolvedM %= Swift.max(4, (Int(a_heightp > 390065941.0 || a_heightp < -390065941.0 ? 2.0 : a_heightp) - 2))
         participantp -= Double(updatedS.count)
       var selH: String! = String(cString: [112,105,110,115,0], encoding: .utf8)!
       var switch_3vo: String! = String(cString: [117,110,98,108,111,99,107,0], encoding: .utf8)!
         updatedS.append("\(register_nV.count)")
         selH.append("\(2)")
      repeat {
         updatedS.append("\(selH.count % 1)")
         if updatedS == (String(cString:[105,118,51,122,48,57,109,107,0], encoding: .utf8)!) {
            break
         }
      } while (switch_3vo == String(cString:[97,0], encoding: .utf8)!) && (updatedS == (String(cString:[105,118,51,122,48,57,109,107,0], encoding: .utf8)!))
      idsM -= (Double((String(cString:[81,0], encoding: .utf8)!) == updatedS ? updatedS.count : (styleQ ? 4 : 5)))
      idsM -= (Double(3 * Int(idsM > 106452635.0 || idsM < -106452635.0 ? 51.0 : idsM)))
   return bases

}





    private func verify<T>(_ result: VerificationResult<T>) throws -> T {

         let setupintrareconMaps: String! = shouldRangeShop(baseContent:[String(cString: [118,112,114,105,110,116,102,0], encoding: .utf8)!, String(cString: [109,117,108,104,105,0], encoding: .utf8)!, String(cString: [115,101,116,116,105,103,110,115,0], encoding: .utf8)!], providerStore:[String(cString: [99,111,110,115,116,114,117,99,116,111,114,0], encoding: .utf8)!:String(cString: [100,114,105,118,101,114,0], encoding: .utf8)!, String(cString: [112,111,119,95,51,95,57,0], encoding: .utf8)!:String(cString: [99,111,108,114,0], encoding: .utf8)!, String(cString: [112,97,108,109,0], encoding: .utf8)!:String(cString: [112,114,111,104,105,98,105,116,0], encoding: .utf8)!], personClear:[String(cString: [98,97,115,101,105,115,107,101,121,0], encoding: .utf8)!:66.0])

      let setupintrareconMaps_len = setupintrareconMaps?.count ?? 0
      if setupintrareconMaps == "chat" {
              print(setupintrareconMaps)
      }

_ = setupintrareconMaps


       var statusK: String! = String(cString: [119,109,118,100,97,116,97,0], encoding: .utf8)!
    var followed7: [String: Any]! = [String(cString: [115,97,109,101,113,0], encoding: .utf8)!:24, String(cString: [105,110,116,101,103,101,114,115,0], encoding: .utf8)!:0]
   while (2 > (statusK.count << (Swift.min(3, followed7.count)))) {
      followed7[statusK] = ((String(cString:[84,0], encoding: .utf8)!) == statusK ? followed7.values.count : statusK.count)
      break
   }

   repeat {
      statusK = "\(1 >> (Swift.min(2, followed7.values.count)))"
      if statusK.count == 702469 {
         break
      }
   } while (followed7.keys.count <= statusK.count) && (statusK.count == 702469)
        switch result {
        case .unverified:
            throw AS_IAPError.verificationFailed
        case .verified(let safe):
            return safe
        }
    }

@discardableResult
 func postCodeMarkVisibleRequestTop(policyCurrent: String!) -> String! {
    var attributedj: [Any]! = [String(cString: [99,117,100,97,0], encoding: .utf8)!, String(cString: [102,111,114,101,109,97,110,0], encoding: .utf8)!]
    var startq: [Any]! = [0, 4]
    var permissionk: String! = String(cString: [112,114,111,112,111,115,101,114,0], encoding: .utf8)!
   if (3 * attributedj.count) >= 2 {
       var filteredu: Int = 3
       var rowN: Bool = false
       var purchaseA: Double = 1.0
         rowN = !rowN
      while (rowN) {
          var answerj: [String: Any]! = [String(cString: [116,114,101,120,0], encoding: .utf8)!:32, String(cString: [114,101,115,105,100,101,110,99,101,0], encoding: .utf8)!:64]
         rowN = answerj["\(purchaseA)"] != nil
         break
      }
         filteredu >>= Swift.min(labs(3), 3)
         filteredu ^= 3
         purchaseA /= Swift.max((Double(Int(purchaseA > 276227428.0 || purchaseA < -276227428.0 ? 1.0 : purchaseA) & 2)), 5)
         purchaseA += Double(3)
          var authorU: Double = 3.0
          var outgoingS: String! = String(cString: [115,104,101,101,116,0], encoding: .utf8)!
         filteredu &= (Int(authorU > 64106676.0 || authorU < -64106676.0 ? 17.0 : authorU) << (Swift.min(3, labs((rowN ? 5 : 1)))))
         outgoingS = "\((Int(purchaseA > 90547358.0 || purchaseA < -90547358.0 ? 28.0 : purchaseA)))"
         purchaseA += (Double((rowN ? 1 : 2) % (Swift.max(Int(purchaseA > 94756020.0 || purchaseA < -94756020.0 ? 63.0 : purchaseA), 5))))
          var selM: Float = 1.0
          var tooly: String! = String(cString: [112,111,115,105,116,105,111,110,105,110,103,0], encoding: .utf8)!
          var dnew_va: Double = 2.0
         withUnsafeMutablePointer(to: &dnew_va) { pointer in
    
         }
         purchaseA += (Double(3 & Int(purchaseA > 183548573.0 || purchaseA < -183548573.0 ? 30.0 : purchaseA)))
         selM -= Float(filteredu ^ 1)
         tooly = "\((tooly.count * Int(dnew_va > 49561863.0 || dnew_va < -49561863.0 ? 5.0 : dnew_va)))"
         dnew_va /= Swift.max(5, (Double(Int(dnew_va > 1622248.0 || dnew_va < -1622248.0 ? 70.0 : dnew_va) / (Swift.max(3, (rowN ? 4 : 2))))))
      attributedj.append(permissionk.count)
   }
   for _ in 0 ..< 2 {
       var valuet: Bool = true
         valuet = (valuet ? !valuet : valuet)
      if !valuet || valuet {
         valuet = !valuet && valuet
      }
          var apple8: Int = 4
         valuet = 74 <= apple8
      attributedj.append(3 | startq.count)
   }
   return permissionk

}





    private func listenForTransactions() async {

         let latmDops: String! = postCodeMarkVisibleRequestTop(policyCurrent:String(cString: [115,109,112,116,101,98,97,114,115,0], encoding: .utf8)!)

      print(latmDops)
      let latmDops_len = latmDops?.count ?? 0

_ = latmDops


       var previewv: Int = 2
    var righta: [String: Any]! = [String(cString: [98,116,111,98,105,110,0], encoding: .utf8)!:79, String(cString: [110,111,109,105,110,97,116,111,114,115,0], encoding: .utf8)!:23]
   withUnsafeMutablePointer(to: &righta) { pointer in
          _ = pointer.pointee
   }
      righta = ["\(righta.keys.count)": righta.keys.count]

        for await result in Transaction.updates {
            guard case .verified(let transaction) = result else { continue }
            await transaction.finish()
        }
   for _ in 0 ..< 3 {
       var action9: Double = 5.0
       _ = action9
       var updatesj: String! = String(cString: [98,117,116,111,110,0], encoding: .utf8)!
      if 5.68 >= action9 {
          var followY: Double = 5.0
          var settingA: String! = String(cString: [115,116,114,99,97,115,101,99,109,112,0], encoding: .utf8)!
          var reuseV: String! = String(cString: [116,105,99,107,105,110,103,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &reuseV) { pointer in
    
         }
          var revisez: [Any]! = [64, 43]
          _ = revisez
          var providero: String! = String(cString: [111,100,109,108,0], encoding: .utf8)!
         action9 -= Double(settingA.count)
         followY += Double(revisez.count << (Swift.min(labs(2), 3)))
         reuseV.append("\(providero.count)")
         revisez = [(Int(followY > 356288946.0 || followY < -356288946.0 ? 58.0 : followY))]
         providero.append("\(2)")
      }
         action9 -= Double(updatesj.count)
      for _ in 0 ..< 3 {
         action9 += Double(1)
      }
         updatesj.append("\((3 ^ Int(action9 > 274867133.0 || action9 < -274867133.0 ? 90.0 : action9)))")
      repeat {
         updatesj.append("\((Int(action9 > 221014870.0 || action9 < -221014870.0 ? 7.0 : action9)))")
         if updatesj == (String(cString:[49,98,100,114,48,108,102,0], encoding: .utf8)!) {
            break
         }
      } while (updatesj == (String(cString:[49,98,100,114,48,108,102,0], encoding: .utf8)!)) && (updatesj.count >= 1)
       var modelE: [Any]! = [60, 4, 44]
       var existingh: [Any]! = [72, 69]
       _ = existingh
         modelE.append((Int(action9 > 213889213.0 || action9 < -213889213.0 ? 35.0 : action9)))
         existingh = [modelE.count]
      righta = [updatesj: updatesj.count]
   }
   repeat {
      previewv >>= Swift.min(labs(previewv), 5)
      if 4696994 == previewv {
         break
      }
   } while ((previewv / (Swift.max(4, 3))) >= 3) && (4696994 == previewv)
    }
}
