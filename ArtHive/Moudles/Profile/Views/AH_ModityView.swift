
import Foundation

import UIKit

protocol PGAH_ApplyInput: AnyObject {
    func profileHeaderViewDidTapWallet(_ view: AH_ModityView)
    func profileHeaderViewDidTapEdit(_ view: AH_ModityView)
    func profileHeaderViewDidTapSetup(_ view: AH_ModityView)
}

class AH_ModityView: UIView {
var with_j: Float? = 0.0
private var keyAppearanceString: String?
var is_Content: Bool? = false




    weak var delegate: PGAH_ApplyInput?

    override init(frame: CGRect) {
        super.init(frame: frame)
        setupUI()
        setupActions()
        configure(
            name: "Marceline",
            age: 20,
            followCount: 30,
            fansCount: 30,
            avatarImage: UIImage(named: "common_empty"),
            coverImage: UIImage(named: "profile_top")
        )
    }

    required init?(coder: NSCoder) {
        super.init(coder: coder)
        setupUI()
        setupActions()
    }

@discardableResult
 func scaleEaseWhite(answerPolicy: Int) -> Bool {
    var captured: Int = 0
    var subtitlen: [Any]! = [74, 70]
    var registeredF: Bool = true
   for _ in 0 ..< 2 {
      subtitlen.append(captured)
   }
       var sessionM: String! = String(cString: [112,114,101,98,117,102,0], encoding: .utf8)!
      repeat {
          var capturem: Bool = false
         withUnsafeMutablePointer(to: &capturem) { pointer in
    
         }
          var baseh: String! = String(cString: [98,101,104,105,110,100,0], encoding: .utf8)!
          var deletedS: Double = 1.0
         sessionM = "\(((capturem ? 4 : 1) | 2))"
         baseh = "\(baseh.count + 3)"
         deletedS /= Swift.max(3, (Double((capturem ? 4 : 2) ^ Int(deletedS > 349378616.0 || deletedS < -349378616.0 ? 82.0 : deletedS))))
         if sessionM == (String(cString:[101,51,108,0], encoding: .utf8)!) {
            break
         }
      } while (!sessionM.hasSuffix("\(sessionM.count)")) && (sessionM == (String(cString:[101,51,108,0], encoding: .utf8)!))
      for _ in 0 ..< 2 {
         sessionM = "\(sessionM.count)"
      }
       var providerj: String! = String(cString: [109,116,105,109,101,0], encoding: .utf8)!
       _ = providerj
         providerj = "\(2 ^ providerj.count)"
      subtitlen.append(subtitlen.count)
      registeredF = !registeredF || !registeredF
   return registeredF

}






    private func setupUI() {

         var aparamsAllocation: Bool = scaleEaseWhite(answerPolicy:67)

      if !aparamsAllocation {
          print("ok")
      }

withUnsafeMutablePointer(to: &aparamsAllocation) { pointer in
    
}


       var overlayL: String! = String(cString: [99,101,108,102,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &overlayL) { pointer in
    
   }
      overlayL.append("\(overlayL.count - 1)")

        addSubview(coverImageView)
        addSubview(avatarImageView)
        addSubview(nameLabel)
        addSubview(ageLabel)
        addSubview(followLabel)
        addSubview(fansLabel)
        addSubview(cardBgImageView)
        cardBgImageView.addSubview(walletButton)
        cardBgImageView.addSubview(editButton)
        cardBgImageView.addSubview(setupButton)
        addSubview(postTitleView)

        coverImageView.snp.makeConstraints { make in
            make.top.leading.trailing.equalToSuperview()
            make.height.equalTo(190)
        }
        avatarImageView.snp.makeConstraints { make in
            make.leading.equalToSuperview().offset(16)
            make.top.equalTo(coverImageView.snp.bottom).offset(-52)
            make.size.equalTo(104)
        }
        nameLabel.snp.makeConstraints { make in
            make.leading.equalTo(avatarImageView.snp.trailing).offset(14)
            make.top.equalTo(coverImageView.snp.bottom).offset(7)
            make.height.equalTo(28)
            make.trailing.lessThanOrEqualTo(ageLabel.snp.leading).offset(-8)
        }
        ageLabel.snp.makeConstraints { make in
            make.leading.equalTo(nameLabel.snp.trailing).offset(8)
            make.centerY.height.equalTo(nameLabel)
            make.trailing.lessThanOrEqualToSuperview().offset(-16)
        }
        followLabel.snp.makeConstraints { make in
            make.leading.equalTo(avatarImageView)
            make.top.equalTo(avatarImageView.snp.bottom).offset(10)
        }
        fansLabel.snp.makeConstraints { make in
            make.leading.equalTo(followLabel.snp.trailing).offset(25)
            make.centerY.equalTo(followLabel)
        }
        cardBgImageView.snp.makeConstraints { make in
            make.leading.trailing.equalToSuperview().inset(16)
            make.top.equalTo(coverImageView.snp.bottom).offset(76)
            make.height.equalTo(180)
        }
        walletButton.snp.makeConstraints { make in
            make.leading.equalToSuperview().offset(8)
            make.top.equalToSuperview().offset(37)
            make.width.equalTo(136)
            make.height.equalTo(129)
        }
        editButton.snp.makeConstraints { make in
            make.trailing.equalToSuperview().offset(-8)
            make.top.equalTo(walletButton)
            make.height.equalTo(60)
        }
        setupButton.snp.makeConstraints { make in
            make.leading.trailing.height.equalTo(editButton)
            make.top.equalTo(editButton.snp.bottom).offset(10)
        }
        postTitleView.snp.makeConstraints { make in
            make.leading.equalToSuperview().offset(16)
            make.top.equalTo(cardBgImageView.snp.bottom).offset(16)
        }
    }

@discardableResult
static func invalidTapSend(sessionConfiguration: [String: Any]!, docLike: Double) -> Bool {
    var applyK: Double = 3.0
    _ = applyK
    var displayU: String! = String(cString: [99,97,118,108,99,0], encoding: .utf8)!
    _ = displayU
    var likedE: Bool = true
   if likedE {
       var character5: [String: Any]! = [String(cString: [102,114,101,113,117,101,110,116,0], encoding: .utf8)!:32, String(cString: [112,111,115,105,116,105,111,110,115,0], encoding: .utf8)!:54, String(cString: [105,110,105,116,104,0], encoding: .utf8)!:0]
       var buttonM: String! = String(cString: [114,101,98,117,105,108,100,0], encoding: .utf8)!
       var users5: String! = String(cString: [114,101,115,99,97,108,101,0], encoding: .utf8)!
       var componentss: String! = String(cString: [99,104,97,114,97,99,116,101,114,105,115,116,105,99,115,0], encoding: .utf8)!
      repeat {
         character5[users5] = componentss.count
         if character5.count == 778877 {
            break
         }
      } while (character5.count == 778877) && (!buttonM.hasPrefix("\(character5.keys.count)"))
         buttonM = "\(buttonM.count)"
      for _ in 0 ..< 2 {
         users5.append("\(2)")
      }
         character5[buttonM] = users5.count % (Swift.max(buttonM.count, 10))
      repeat {
         buttonM.append("\(buttonM.count)")
         if (String(cString:[108,97,103,56,0], encoding: .utf8)!) == buttonM {
            break
         }
      } while ((String(cString:[108,97,103,56,0], encoding: .utf8)!) == buttonM) && ((2 | character5.values.count) > 3 || 5 > (2 | character5.values.count))
         character5 = [componentss: ((String(cString:[50,0], encoding: .utf8)!) == buttonM ? componentss.count : buttonM.count)]
      while (users5.count >= character5.count) {
         users5.append("\(componentss.count)")
         break
      }
         buttonM.append("\(buttonM.count)")
      repeat {
         users5.append("\(character5.keys.count + componentss.count)")
         if users5 == (String(cString:[101,101,52,110,104,57,115,98,55,0], encoding: .utf8)!) {
            break
         }
      } while (users5 == (String(cString:[101,101,52,110,104,57,115,98,55,0], encoding: .utf8)!)) && (users5.count <= 4)
       var fansS: Double = 4.0
       var base2: Double = 4.0
         buttonM.append("\(users5.count)")
         buttonM.append("\(componentss.count)")
         fansS -= Double(componentss.count)
         base2 /= Swift.max(3, (Double(Int(base2 > 151278924.0 || base2 < -151278924.0 ? 66.0 : base2) % 2)))
      displayU = "\(3)"
   }
   for _ in 0 ..< 1 {
      applyK -= (Double(2 % (Swift.max(1, Int(applyK > 142400583.0 || applyK < -142400583.0 ? 46.0 : applyK)))))
   }
   while (!displayU.contains("\(likedE)")) {
       var pathF: [String: Any]! = [String(cString: [100,101,109,117,120,101,114,115,0], encoding: .utf8)!:[String(cString: [99,117,116,111,117,116,0], encoding: .utf8)!, String(cString: [118,112,99,120,0], encoding: .utf8)!]]
      withUnsafeMutablePointer(to: &pathF) { pointer in
    
      }
         pathF = ["\(pathF.keys.count)": pathF.keys.count - 2]
          var followedJ: String! = String(cString: [99,111,110,118,101,114,103,101,110,99,101,0], encoding: .utf8)!
          _ = followedJ
          var p_widthG: Double = 1.0
          _ = p_widthG
          var dotI: Bool = true
         pathF = ["\(pathF.count)": 3 * pathF.values.count]
         followedJ = "\(3)"
         p_widthG += Double(2 >> (Swift.min(3, pathF.count)))
         dotI = dotI && p_widthG == 21.93
      for _ in 0 ..< 1 {
         pathF = ["\(pathF.keys.count)": pathF.values.count / (Swift.max(pathF.keys.count, 3))]
      }
      likedE = !likedE || displayU.count <= 72
      break
   }
   return likedE

}






    static func height(for width: CGFloat) -> CGFloat {

         var lavuLine: Bool = invalidTapSend(sessionConfiguration:[String(cString: [112,114,111,114,101,115,100,97,116,97,0], encoding: .utf8)!:23], docLike:33.0)

      if !lavuLine {
          print("art")
      }

withUnsafeMutablePointer(to: &lavuLine) { pointer in
    
}


       var pendingm: String! = String(cString: [114,101,113,117,101,115,116,105,110,103,0], encoding: .utf8)!
    _ = pendingm
   repeat {
      pendingm.append("\(pendingm.count)")
      if pendingm.count == 2400713 {
         break
      }
   } while (pendingm == String(cString:[98,0], encoding: .utf8)! || 1 >= pendingm.count) && (pendingm.count == 2400713)

        let applicationHeader = AH_ModityView(frame: CGRect(x: 0, y: 0, width: width, height: 0))
        return applicationHeader.systemLayoutSizeFitting(
            CGSize(width: width, height: UIView.layoutFittingCompressedSize.height),
            withHorizontalFittingPriority: .required,
            verticalFittingPriority: .fittingSizeLevel
        ).height
    }

@discardableResult
 func transitionGreenGenerator(completionMerge: [Any]!, filteredConfig: String!, pathCopy_gm: [String: Any]!) -> String! {
    var detailv: Double = 4.0
    var selu: String! = String(cString: [115,117,98,115,97,109,112,0], encoding: .utf8)!
    var w_positionR: String! = String(cString: [116,105,109,101,111,117,116,101,100,0], encoding: .utf8)!
   repeat {
      detailv += Double(2)
      if 587744.0 == detailv {
         break
      }
   } while (selu.count < 3) && (587744.0 == detailv)
   for _ in 0 ..< 3 {
      w_positionR = "\((3 ^ Int(detailv > 349324890.0 || detailv < -349324890.0 ? 45.0 : detailv)))"
   }
      w_positionR = "\(selu.count / (Swift.max(w_positionR.count, 10)))"
   return selu

}






    private func setupActions() {

         var gamesTilex: String! = transitionGreenGenerator(completionMerge:[21.0], filteredConfig:String(cString: [117,109,105,100,0], encoding: .utf8)!, pathCopy_gm:[String(cString: [99,104,111,112,0], encoding: .utf8)!:5.0])

      if gamesTilex == "remove" {
              print(gamesTilex)
      }
      let gamesTilex_len = gamesTilex?.count ?? 0

withUnsafeMutablePointer(to: &gamesTilex) { pointer in
        _ = pointer.pointee
}


       var generators: String! = String(cString: [108,105,99,101,110,115,101,0], encoding: .utf8)!
   repeat {
      generators.append("\(generators.count)")
      if generators == (String(cString:[51,99,110,49,105,100,122,0], encoding: .utf8)!) {
         break
      }
   } while (generators == (String(cString:[51,99,110,49,105,100,122,0], encoding: .utf8)!)) && (generators.hasSuffix(generators))

        walletButton.addTarget(self, action: #selector(clickWalletButton), for: .touchUpInside)
        editButton.addTarget(self, action: #selector(clickEditButton), for: .touchUpInside)
        setupButton.addTarget(self, action: #selector(clickSetupButton), for: .touchUpInside)
    }

@discardableResult
 func characterReportStorageCustom() -> [Any]! {
    var delegate_qC: String! = String(cString: [115,116,114,101,116,99,104,0], encoding: .utf8)!
    var e_centerC: String! = String(cString: [99,112,117,117,115,101,100,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &e_centerC) { pointer in
    
   }
    var default_2q: [Any]! = [74, 81]
      e_centerC.append("\(delegate_qC.count)")
      e_centerC = "\(((String(cString:[65,0], encoding: .utf8)!) == e_centerC ? e_centerC.count : default_2q.count))"
      default_2q = [(e_centerC == (String(cString:[72,0], encoding: .utf8)!) ? default_2q.count : e_centerC.count)]
   return default_2q

}






    @objc private func clickWalletButton() {

         var x_4Ads: [Any]! = characterReportStorageCustom()

      let x_4Ads_len = x_4Ads.count
      x_4Ads.forEach({ (obj) in
          print(obj)
      })

withUnsafeMutablePointer(to: &x_4Ads) { pointer in
    
}


       var registeredP: Int = 4
    var cleanedW: Double = 5.0
       var right4: Double = 1.0
         right4 += (Double(Int(right4 > 221118327.0 || right4 < -221118327.0 ? 47.0 : right4)))
         right4 -= (Double(Int(right4 > 114702154.0 || right4 < -114702154.0 ? 7.0 : right4) >> (Swift.min(1, labs(2)))))
      while (5.9 == (right4 / (Swift.max(right4, 3))) || (right4 / (Swift.max(1, right4))) == 5.9) {
          var locationS: Double = 2.0
          var controller4: Double = 3.0
          var launchx: String! = String(cString: [116,97,112,116,105,99,0], encoding: .utf8)!
          var migratew: String! = String(cString: [116,114,97,100,101,0], encoding: .utf8)!
          var subtitleq: [Any]! = [71, 68, 59]
         withUnsafeMutablePointer(to: &subtitleq) { pointer in
    
         }
         right4 -= (Double(Int(right4 > 155428039.0 || right4 < -155428039.0 ? 8.0 : right4) + Int(locationS > 10004775.0 || locationS < -10004775.0 ? 82.0 : locationS)))
         controller4 += Double(migratew.count << (Swift.min(2, subtitleq.count)))
         launchx.append("\(1 % (Swift.max(9, subtitleq.count)))")
         migratew = "\(launchx.count)"
         break
      }
      cleanedW += Double(registeredP)
   repeat {
      registeredP |= registeredP % 3
      if 1298926 == registeredP {
         break
      }
   } while (1298926 == registeredP) && ((registeredP % 2) == 5)

        delegate?.profileHeaderViewDidTapWallet(self)
    }

@discardableResult
 func ensureValueRadius(providerDirectory: Int) -> Double {
    var layoutx: Int = 5
    var cachedO: Bool = false
    var closeR: Double = 5.0
   repeat {
      cachedO = closeR <= 50.93 && !cachedO
      if cachedO ? !cachedO : cachedO {
         break
      }
   } while (layoutx >= 2) && (cachedO ? !cachedO : cachedO)
      cachedO = closeR >= 58.67 && cachedO
   return closeR

}






    func configure(
        name: String,
        age: Int,
        followCount: Int,
        fansCount: Int,
        avatarImage: UIImage?,
        coverImage: UIImage?
    ) {

         let expireConversation: Double = ensureValueRadius(providerDirectory:23)

      print(expireConversation)

_ = expireConversation


       var playe: Double = 3.0
    var previewz: String! = String(cString: [115,105,110,107,115,0], encoding: .utf8)!
       var animationsg: Int = 4
       var clearz: [Any]! = [false]
       var fullC: Int = 3
      while ((fullC + clearz.count) == 1 || 1 == (clearz.count + fullC)) {
         clearz.append(clearz.count ^ 3)
         break
      }
      if 3 == (animationsg / (Swift.max(1, 8))) {
          var confirmedS: String! = String(cString: [99,101,110,116,101,114,105,110,103,0], encoding: .utf8)!
         animationsg /= Swift.max(clearz.count - fullC, 1)
         confirmedS.append("\(confirmedS.count * fullC)")
      }
         animationsg >>= Swift.min(1, labs(1 & clearz.count))
          var closew: String! = String(cString: [109,98,108,111,99,107,0], encoding: .utf8)!
          var userw: Double = 0.0
         withUnsafeMutablePointer(to: &userw) { pointer in
                _ = pointer.pointee
         }
         clearz.append(animationsg)
         closew.append("\(2)")
         userw += Double(fullC)
          var address_: String! = String(cString: [108,111,97,116,0], encoding: .utf8)!
          _ = address_
          var captureg: String! = String(cString: [115,105,110,117,115,111,105,100,97,108,0], encoding: .utf8)!
          var creatorh: String! = String(cString: [115,105,122,101,98,105,116,114,97,116,101,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &creatorh) { pointer in
                _ = pointer.pointee
         }
         animationsg |= animationsg | address_.count
         captureg.append("\(creatorh.count)")
         creatorh = "\(1)"
      if (5 * animationsg) < 3 {
         clearz.append(fullC >> (Swift.min(clearz.count, 5)))
      }
      if (1 ^ animationsg) <= 5 {
         animationsg %= Swift.max(5, clearz.count)
      }
      if (fullC << (Swift.min(labs(animationsg), 2))) >= 2 {
         animationsg ^= clearz.count
      }
         clearz.append(fullC & 3)
      previewz.append("\((1 * Int(playe > 93803334.0 || playe < -93803334.0 ? 10.0 : playe)))")

   repeat {
       var deviceL: [Any]! = [69, 86]
      withUnsafeMutablePointer(to: &deviceL) { pointer in
             _ = pointer.pointee
      }
       var reviseM: String! = String(cString: [101,110,116,114,111,112,121,0], encoding: .utf8)!
      repeat {
         deviceL = [reviseM.count]
         if 3349291 == deviceL.count {
            break
         }
      } while (3349291 == deviceL.count) && ((reviseM.count % 3) >= 2 && 3 >= (deviceL.count % 3))
         deviceL.append(2 - reviseM.count)
         deviceL.append(deviceL.count)
      if 1 >= (4 >> (Swift.min(1, reviseM.count))) {
         deviceL = [reviseM.count]
      }
      while (4 > (reviseM.count / 3)) {
          var o_positionx: Double = 1.0
         reviseM = "\((Int(o_positionx > 264698412.0 || o_positionx < -264698412.0 ? 76.0 : o_positionx)))"
         break
      }
         reviseM = "\(1)"
      previewz.append("\((Int(playe > 284223856.0 || playe < -284223856.0 ? 99.0 : playe) ^ 1))")
      if previewz == (String(cString:[103,95,112,95,102,105,118,95,53,0], encoding: .utf8)!) {
         break
      }
   } while (Double(previewz.count) > playe) && (previewz == (String(cString:[103,95,112,95,102,105,118,95,53,0], encoding: .utf8)!))
        avatarImageView.image = avatarImage
        coverImageView.image = coverImage ?? UIImage(named: "profile_top")
        nameLabel.text = name
        ageLabel.text = "\(age) years old"
        followLabel.attributedText = Self.statText(title: "Follow", count: followCount)
        fansLabel.attributedText = Self.statText(title: "Fans", count: fansCount)
    }


    @objc private func clickSetupButton() {
       var locationL: String! = String(cString: [99,97,114,111,117,115,101,108,0], encoding: .utf8)!
    _ = locationL
   while (locationL.hasPrefix("\(locationL.count)")) {
       var launchG: Int = 1
       _ = launchG
       var likedA: Int = 4
      withUnsafeMutablePointer(to: &likedA) { pointer in
    
      }
      while (likedA >= launchG) {
         likedA |= 2 << (Swift.min(3, labs(likedA)))
         break
      }
      if (launchG % (Swift.max(6, likedA))) > 2 {
         likedA /= Swift.max(2, likedA | 3)
      }
       var generatort: [String: Any]! = [String(cString: [109,97,107,101,116,97,114,98,97,108,108,0], encoding: .utf8)!:String(cString: [101,95,55,53,0], encoding: .utf8)!, String(cString: [115,99,97,108,97,98,105,108,105,116,121,0], encoding: .utf8)!:String(cString: [100,117,115,116,0], encoding: .utf8)!]
       var blackt: [String: Any]! = [String(cString: [109,111,118,101,116,111,0], encoding: .utf8)!:8, String(cString: [101,110,99,111,100,101,100,115,116,114,101,97,109,0], encoding: .utf8)!:83]
          var dismissF: Double = 1.0
          var postI: String! = String(cString: [116,114,117,110,99,97,116,101,100,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &postI) { pointer in
    
         }
         launchG |= blackt.keys.count
         dismissF -= Double(launchG | 2)
         postI = "\(postI.count ^ 3)"
         generatort["\(likedA)"] = likedA
          var loading7: String! = String(cString: [115,117,114,102,97,99,101,115,0], encoding: .utf8)!
          var main_aA: [String: Any]! = [String(cString: [105,100,99,116,120,0], encoding: .utf8)!:String(cString: [103,110,111,115,105,115,0], encoding: .utf8)!, String(cString: [97,108,112,104,97,110,117,109,0], encoding: .utf8)!:String(cString: [109,98,109,111,100,101,0], encoding: .utf8)!, String(cString: [120,102,97,99,101,0], encoding: .utf8)!:String(cString: [121,117,118,109,112,101,103,0], encoding: .utf8)!]
         blackt = ["\(main_aA.count)": 2]
         loading7.append("\(loading7.count | 1)")
      locationL.append("\((locationL == (String(cString:[98,0], encoding: .utf8)!) ? locationL.count : likedA))")
      break
   }

        delegate?.profileHeaderViewDidTapSetup(self)
    }

@discardableResult
 func captionRootLaunchSecureDissolve() -> [Any]! {
    var pickerg: String! = String(cString: [100,111,119,110,115,116,114,101,97,109,0], encoding: .utf8)!
    _ = pickerg
    var cellX: Double = 4.0
    var applyj: [Any]! = [34]
   withUnsafeMutablePointer(to: &applyj) { pointer in
          _ = pointer.pointee
   }
   if pickerg.hasSuffix("\(cellX)") {
      pickerg = "\(applyj.count | 1)"
   }
      cellX -= Double(3 + pickerg.count)
   while (5 > (1 / (Swift.max(8, applyj.count)))) {
      applyj.append(applyj.count)
      break
   }
   return applyj

}






    @objc private func clickEditButton() {

         var sitemapVpdec: [Any]! = captionRootLaunchSecureDissolve()

      let sitemapVpdec_len = sitemapVpdec.count
      sitemapVpdec.forEach({ (obj) in
          print(obj)
      })

withUnsafeMutablePointer(to: &sitemapVpdec) { pointer in
    
}


       var showg: [String: Any]! = [String(cString: [107,105,108,108,0], encoding: .utf8)!:String(cString: [101,120,110,111,100,101,0], encoding: .utf8)!, String(cString: [108,105,110,101,98,114,101,97,107,0], encoding: .utf8)!:String(cString: [112,114,111,99,101,101,100,0], encoding: .utf8)!, String(cString: [115,108,111,116,115,0], encoding: .utf8)!:String(cString: [100,99,112,114,101,100,0], encoding: .utf8)!]
   while (1 < (showg.count & 2)) {
      showg = ["\(showg.count)": 1 + showg.keys.count]
      break
   }

        delegate?.profileHeaderViewDidTapEdit(self)
    }

@discardableResult
static func startFilterCustomFinish(discoverIndex: String!, rowHome: Bool) -> String! {
    var headerO: Double = 3.0
    var release_c_M: Bool = false
    _ = release_c_M
    var maili: String! = String(cString: [102,111,108,100,101,114,115,0], encoding: .utf8)!
   repeat {
      maili.append("\(maili.count)")
      if 2115458 == maili.count {
         break
      }
   } while (maili.count <= 3) && (2115458 == maili.count)
   repeat {
       var interval_sB: [Any]! = [59, 55]
       var tabbar9: Bool = false
         tabbar9 = interval_sB.count == 33 || tabbar9
      for _ in 0 ..< 2 {
          var nameO: Int = 3
          var size_nf4: Double = 4.0
          _ = size_nf4
          var ageI: Bool = false
         withUnsafeMutablePointer(to: &ageI) { pointer in
    
         }
         interval_sB = [(Int(size_nf4 > 60391171.0 || size_nf4 < -60391171.0 ? 33.0 : size_nf4) | 3)]
         nameO /= Swift.max(((ageI ? 4 : 1) ^ Int(size_nf4 > 253899456.0 || size_nf4 < -253899456.0 ? 72.0 : size_nf4)), 3)
         ageI = 100.14 >= size_nf4
      }
      for _ in 0 ..< 1 {
         interval_sB = [(1 ^ (tabbar9 ? 4 : 4))]
      }
         tabbar9 = (10 < (interval_sB.count >> (Swift.min(3, labs((tabbar9 ? 10 : interval_sB.count))))))
      repeat {
         interval_sB = [((tabbar9 ? 4 : 3) - 3)]
         if 537845 == interval_sB.count {
            break
         }
      } while (interval_sB.count == 2) && (537845 == interval_sB.count)
      for _ in 0 ..< 1 {
         interval_sB = [interval_sB.count % 1]
      }
      headerO -= (Double(interval_sB.count / (Swift.max(7, (release_c_M ? 1 : 5)))))
      if 2211287.0 == headerO {
         break
      }
   } while (2211287.0 == headerO) && (headerO >= Double(maili.count))
      maili = "\(((String(cString:[79,0], encoding: .utf8)!) == maili ? (release_c_M ? 1 : 5) : maili.count))"
   return maili

}






    private static func statText(title: String, count: Int) -> NSAttributedString {

         var savingVbrush: String! = startFilterCustomFinish(discoverIndex:String(cString: [104,116,109,108,109,97,114,107,117,112,0], encoding: .utf8)!, rowHome:true)

      if savingVbrush == "shared" {
              print(savingVbrush)
      }
      let savingVbrush_len = savingVbrush?.count ?? 0

withUnsafeMutablePointer(to: &savingVbrush) { pointer in
        _ = pointer.pointee
}


        let button = "\(title) \(count)"
        let target = NSMutableAttributedString(
            string: button,
            attributes: [
                .font: UIFont.systemFont(ofSize: 14),
                .foregroundColor: UIColor.black
            ]
        )
        let deleted = "\(count)"
        if let range = button.range(of: deleted) {
            let conversations = NSRange(range, in: button)
            target.addAttributes(
                [.font: UIFont.systemFont(ofSize: 14, weight: .bold)],
                range: conversations
            )
        }
        return target
    }

    private let coverImageView: UIImageView = {
       var d_imageJ: Int = 1
    var signe: Int = 2
    _ = signe
      signe &= d_imageJ

        let view = UIImageView()
      signe /= Swift.max(4, d_imageJ * signe)
        view.image = UIImage(named: "profile_top")
        view.contentMode = .scaleAspectFill
        view.clipsToBounds = true
        return view
    }()

    private let cardBgImageView: UIImageView = {
       var w_viewf: String! = String(cString: [118,112,100,97,116,97,0], encoding: .utf8)!
       var interval_g9: [Any]! = [String(cString: [97,110,105,109,97,116,105,111,110,115,0], encoding: .utf8)!, String(cString: [112,108,97,121,101,100,0], encoding: .utf8)!, String(cString: [107,105,99,107,0], encoding: .utf8)!]
       var m_playerZ: String! = String(cString: [112,97,116,104,110,97,109,101,0], encoding: .utf8)!
       var label0: Double = 2.0
          var storage6: Int = 3
         m_playerZ.append("\((Int(label0 > 3571055.0 || label0 < -3571055.0 ? 41.0 : label0)))")
         storage6 ^= (Int(label0 > 118904606.0 || label0 < -118904606.0 ? 83.0 : label0))
      while (label0 >= 4.5) {
          var attachmentp: [Any]! = [29, 84]
          var indicatoro: Double = 3.0
          var purchasingp: Float = 3.0
          var followedU: Double = 2.0
         withUnsafeMutablePointer(to: &followedU) { pointer in
                _ = pointer.pointee
         }
         m_playerZ.append("\(3)")
         attachmentp = [1]
         indicatoro += (Double(Int(followedU > 362203215.0 || followedU < -362203215.0 ? 45.0 : followedU)))
         purchasingp += (Float(Int(followedU > 10457616.0 || followedU < -10457616.0 ? 44.0 : followedU) ^ Int(purchasingp > 290451397.0 || purchasingp < -290451397.0 ? 23.0 : purchasingp)))
         break
      }
       var chat0: Int = 5
       _ = chat0
       var storeu: Int = 0
      if 3 > (chat0 | 2) || (chat0 | interval_g9.count) > 2 {
          var intro6: String! = String(cString: [114,101,116,114,97,110,115,109,105,115,115,105,111,110,0], encoding: .utf8)!
          var blacklistedF: String! = String(cString: [115,101,108,101,99,116,97,98,108,101,0], encoding: .utf8)!
          var directorym: [String: Any]! = [String(cString: [108,97,117,110,99,104,101,100,0], encoding: .utf8)!:String(cString: [115,121,109,98,111,108,0], encoding: .utf8)!]
          _ = directorym
         interval_g9 = [directorym.keys.count]
         intro6.append("\(1 % (Swift.max(3, chat0)))")
         blacklistedF = "\(intro6.count)"
      }
      if (label0 + 1.90) > 5.73 || (2 ^ storeu) > 1 {
          var modityV: String! = String(cString: [97,117,116,111,100,101,108,101,116,101,0], encoding: .utf8)!
          var privacyp: Bool = false
          var cornerH: String! = String(cString: [97,99,99,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &cornerH) { pointer in
    
         }
         storeu |= ((privacyp ? 4 : 1) % 2)
         modityV = "\(modityV.count)"
         cornerH = "\(m_playerZ.count << (Swift.min(labs(2), 2)))"
      }
       var statusD: String! = String(cString: [97,117,116,111,114,101,115,105,122,101,115,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &statusD) { pointer in
             _ = pointer.pointee
      }
       var userj: String! = String(cString: [119,97,118,101,0], encoding: .utf8)!
      if (chat0 >> (Swift.min(3, labs(storeu)))) > 3 || (3 >> (Swift.min(1, labs(storeu)))) > 2 {
         chat0 /= Swift.max(5, 2)
      }
      for _ in 0 ..< 3 {
         statusD = "\(1 << (Swift.min(4, labs(chat0))))"
      }
      while (!interval_g9.contains { $0 as? Int == chat0 }) {
         interval_g9 = [chat0 + statusD.count]
         break
      }
         userj = "\(interval_g9.count)"
      w_viewf.append("\(w_viewf.count)")

        let view = UIImageView()
        view.image = UIImage(named: "profile_cardBg")
        view.contentMode = .scaleToFill
        view.isUserInteractionEnabled = true
        return view
    }()

    private let avatarImageView: UIImageView = {
       var text8: String! = String(cString: [99,111,110,118,101,114,116,0], encoding: .utf8)!
    var lan0: String! = String(cString: [98,115,119,97,112,100,115,112,0], encoding: .utf8)!
      text8 = "\(lan0.count)"

        let view = UIImageView()
   for _ in 0 ..< 1 {
      lan0.append("\(text8.count | 1)")
   }
        view.contentMode = .scaleAspectFill
        view.layer.cornerRadius = 16
        view.layer.masksToBounds = true
        view.layer.borderWidth = 2
        view.layer.borderColor = UIColor.white.cgColor
        return view
    }()

    private let nameLabel: UILabel = {
       var main_iJ: String! = String(cString: [98,101,103,97,110,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &main_iJ) { pointer in
          _ = pointer.pointee
   }
   repeat {
       var datel: [Any]! = [35, 1]
       _ = datel
       var dotq: String! = String(cString: [99,104,114,111,109,97,109,99,0], encoding: .utf8)!
       var network3: String! = String(cString: [99,97,108,99,119,0], encoding: .utf8)!
       _ = network3
       var savem: Bool = false
       var lanV: Double = 1.0
      for _ in 0 ..< 2 {
         datel.append((3 | Int(lanV > 314138524.0 || lanV < -314138524.0 ? 13.0 : lanV)))
      }
      while (2 >= (datel.count % 3)) {
         savem = 80 > datel.count
         break
      }
          var capturez: Int = 0
          var inputH: Int = 5
         dotq = "\(dotq.count)"
         capturez += 2
         inputH -= capturez + inputH
         savem = (((!savem ? 43 : dotq.count) / (Swift.max(dotq.count, 7))) >= 43)
         savem = network3.count < 76 && savem
      repeat {
          var senderc: Int = 3
         withUnsafeMutablePointer(to: &senderc) { pointer in
    
         }
         lanV /= Swift.max(Double(2), 3)
         senderc |= (3 >> (Swift.min(5, labs(Int(lanV > 201345753.0 || lanV < -201345753.0 ? 91.0 : lanV)))))
         if 2331049.0 == lanV {
            break
         }
      } while (lanV == 3.58) && (2331049.0 == lanV)
      for _ in 0 ..< 2 {
         lanV -= Double(2 >> (Swift.min(4, network3.count)))
      }
      for _ in 0 ..< 1 {
          var checked_: String! = String(cString: [115,101,99,117,114,101,100,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &checked_) { pointer in
                _ = pointer.pointee
         }
          var paragraph9: String! = String(cString: [116,104,101,114,101,0], encoding: .utf8)!
         network3 = "\(2)"
         checked_ = "\(1 | paragraph9.count)"
         paragraph9.append("\(1 - network3.count)")
      }
       var answersr: Double = 1.0
       var headerX: Double = 4.0
      while ((3 * Int(lanV > 45285550.0 || lanV < -45285550.0 ? 65.0 : lanV)) > 5 && (dotq.count * 3) > 1) {
         dotq.append("\(2)")
         break
      }
      while (network3.hasSuffix("\(answersr)")) {
          var followQ: Int = 2
          var indicatorq: Double = 2.0
         answersr += (Double(Int(headerX > 203720942.0 || headerX < -203720942.0 ? 69.0 : headerX)))
         followQ ^= ((String(cString:[116,0], encoding: .utf8)!) == network3 ? network3.count : Int(lanV > 137713701.0 || lanV < -137713701.0 ? 55.0 : lanV))
         indicatorq -= (Double((savem ? 1 : 2) - datel.count))
         break
      }
          var modityI: String! = String(cString: [112,97,97,100,0], encoding: .utf8)!
          var likeM: String! = String(cString: [108,117,109,97,107,101,121,0], encoding: .utf8)!
          var components0: [String: Any]! = [String(cString: [99,108,111,117,100,102,108,97,114,101,0], encoding: .utf8)!:[0, 79, 64]]
         network3 = "\(2 << (Swift.min(2, modityI.count)))"
         likeM.append("\((Int(answersr > 224548235.0 || answersr < -224548235.0 ? 13.0 : answersr) << (Swift.min(4, labs((savem ? 2 : 5))))))")
         components0[network3] = 1 & network3.count
          var pickerE: Double = 1.0
          var directoryT: Bool = true
          var peopleD: Int = 3
          _ = peopleD
         savem = 80.92 == lanV
         pickerE += (Double(Int(headerX > 7458485.0 || headerX < -7458485.0 ? 26.0 : headerX) * 2))
         directoryT = network3.count <= 53 && savem
         peopleD >>= Swift.min(labs((dotq == (String(cString:[67,0], encoding: .utf8)!) ? dotq.count : Int(lanV > 101481197.0 || lanV < -101481197.0 ? 17.0 : lanV))), 2)
      while ((network3.count | 1) == 5 && 2.43 == (4.97 + answersr)) {
          var bindQ: String! = String(cString: [99,111,112,121,120,110,0], encoding: .utf8)!
          var mergeG: [String: Any]! = [String(cString: [109,112,99,104,117,102,102,0], encoding: .utf8)!:[89, 54, 7]]
          var profileK: [String: Any]! = [String(cString: [99,104,97,114,99,111,110,118,0], encoding: .utf8)!:79, String(cString: [114,101,108,101,118,97,110,116,0], encoding: .utf8)!:61]
          var applev: Int = 4
          var indexS: [Any]! = [23, 17]
         withUnsafeMutablePointer(to: &indexS) { pointer in
                _ = pointer.pointee
         }
         network3 = "\(1 ^ bindQ.count)"
         mergeG["\(headerX)"] = (Int(headerX > 223362770.0 || headerX < -223362770.0 ? 15.0 : headerX))
         profileK[dotq] = datel.count
         applev += ((savem ? 1 : 3) % 3)
         indexS.append(bindQ.count)
         break
      }
         savem = lanV < answersr
      main_iJ.append("\((Int(lanV > 5593921.0 || lanV < -5593921.0 ? 20.0 : lanV) / (Swift.max(datel.count, 6))))")
      if 4386929 == main_iJ.count {
         break
      }
   } while (main_iJ.hasSuffix("\(main_iJ.count)")) && (4386929 == main_iJ.count)

        let label = UILabel()
        label.font = .systemFont(ofSize: 20, weight: .bold)
        label.textColor = .black
        return label
    }()

    private let ageLabel: UILabel = {
       var componentsO: Bool = false
    var lanv: [String: Any]! = [String(cString: [97,110,105,109,97,116,111,114,115,0], encoding: .utf8)!:15.0]
   withUnsafeMutablePointer(to: &lanv) { pointer in
    
   }
      lanv = ["\(lanv.values.count)": 1]

        let label = UILabel()
   for _ in 0 ..< 3 {
      componentsO = ((lanv.keys.count & (!componentsO ? 57 : lanv.keys.count)) == 57)
   }
        label.font = .systemFont(ofSize: 14, weight: .medium)
        label.textColor = UIColor(hex: "#FF845F")
        label.setContentCompressionResistancePriority(.required, for: .horizontal)
        return label
    }()

    private let followLabel = UILabel()

    private let fansLabel = UILabel()

    private let walletButton: UIButton = {
       var updatedw: String! = String(cString: [102,111,108,108,111,119,101,114,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &updatedw) { pointer in
          _ = pointer.pointee
   }
    var attributedk: String! = String(cString: [99,111,101,102,115,0], encoding: .utf8)!
   if 5 <= attributedk.count || updatedw.count <= 5 {
      updatedw = "\(((String(cString:[111,0], encoding: .utf8)!) == attributedk ? attributedk.count : updatedw.count))"
   }

        let button = UIButton(type: .custom)
      updatedw = "\(updatedw.count + 1)"
        button.setImage(UIImage(named: "profile_wallet"), for: .normal)
        button.imageView?.contentMode = .scaleAspectFill
        return button
    }()

    private let editButton: UIButton = {
       var remove4: String! = String(cString: [115,101,114,118,101,114,115,0], encoding: .utf8)!
    _ = remove4
   if remove4.count >= remove4.count {
      remove4 = "\(remove4.count ^ 2)"
   }

        let button = UIButton(type: .custom)
        button.setImage(UIImage(named: "profile_edit"), for: .normal)
        button.imageView?.contentMode = .scaleAspectFill
        return button
    }()

    private let setupButton: UIButton = {
       var attachmentP: Bool = true
      attachmentP = (attachmentP ? attachmentP : attachmentP)

        let button = UIButton(type: .custom)
        button.setImage(UIImage(named: "profile_setup"), for: .normal)
        button.imageView?.contentMode = .scaleAspectFill
        return button
    }()

    private let postTitleView: UIImageView = {
       var loggedZ: Double = 0.0
    var buyl: String! = String(cString: [116,104,111,117,103,104,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &buyl) { pointer in
          _ = pointer.pointee
   }
   if 2.20 == loggedZ {
       var anew_aT: Double = 4.0
       var viewsp: String! = String(cString: [117,115,101,114,99,116,120,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &viewsp) { pointer in
             _ = pointer.pointee
      }
         anew_aT += (Double(Int(anew_aT > 158353290.0 || anew_aT < -158353290.0 ? 3.0 : anew_aT) + 3))
      repeat {
         viewsp = "\((1 + Int(anew_aT > 3648406.0 || anew_aT < -3648406.0 ? 69.0 : anew_aT)))"
         if (String(cString:[51,54,100,111,54,56,0], encoding: .utf8)!) == viewsp {
            break
         }
      } while ((1 + viewsp.count) > 3) && ((String(cString:[51,54,100,111,54,56,0], encoding: .utf8)!) == viewsp)
         viewsp.append("\((Int(anew_aT > 318927290.0 || anew_aT < -318927290.0 ? 55.0 : anew_aT)))")
      while ((2 ^ viewsp.count) >= 2 && (viewsp.count + Int(anew_aT > 343748882.0 || anew_aT < -343748882.0 ? 84.0 : anew_aT)) >= 2) {
         viewsp = "\((viewsp == (String(cString:[100,0], encoding: .utf8)!) ? Int(anew_aT > 292289105.0 || anew_aT < -292289105.0 ? 94.0 : anew_aT) : viewsp.count))"
         break
      }
      if anew_aT == Double(viewsp.count) {
         viewsp = "\((Int(anew_aT > 73959678.0 || anew_aT < -73959678.0 ? 40.0 : anew_aT) - viewsp.count))"
      }
      if 3 <= (viewsp.count & 1) && (1 << (Swift.min(4, viewsp.count))) <= 4 {
         anew_aT -= Double(viewsp.count)
      }
      loggedZ += (Double(Int(anew_aT > 90211662.0 || anew_aT < -90211662.0 ? 1.0 : anew_aT) * 2))
   }

        let view = UIImageView()
   if 2 == (2 % (Swift.max(4, buyl.count))) && 4.64 == (Double(buyl.count) + loggedZ) {
      buyl.append("\((Int(loggedZ > 265637750.0 || loggedZ < -265637750.0 ? 13.0 : loggedZ)))")
   }
        view.image = UIImage(named: "profile_post")
        view.contentMode = .scaleAspectFill
        return view
    }()
}
