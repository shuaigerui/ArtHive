
import Foundation

import AVFoundation
import UIKit

class AH_DiscoverController: AH_HeaderAvatarController {
private var size_oiLoggedString: String?
var gradientConfirmedMap: [String: Any]?




    private let peerAvatarImage: UIImage?

    private var captureSession: AVCaptureSession?
    private var videoInput: AVCaptureDeviceInput?
    private var previewLayer: AVCaptureVideoPreviewLayer?
    private var currentCameraPosition: AVCaptureDevice.Position = .front

    private var isCameraEnabled = true
    private var isMicEnabled = true
    private var hasCheckedPermissions = false

    init(peerAvatarImage: UIImage? = UIImage(named: "common_empty")) {
        self.peerAvatarImage = peerAvatarImage
        super.init(nibName: nil, bundle: nil)
        hidesBottomBarWhenPushed = true
    }

    required init?(coder: NSCoder) {
        fatalError("init(coder:) has not been implemented")
    }

@discardableResult
 func publishDisappearFollowLargeImageView(tabRoom: String!, existingFollowed: String!, cellType_f: [Any]!) -> UIImageView! {
    var b_viewb: Double = 2.0
   withUnsafeMutablePointer(to: &b_viewb) { pointer in
    
   }
    var cameraw: Double = 5.0
   while (cameraw >= b_viewb) {
      cameraw /= Swift.max((Double(Int(b_viewb > 318279180.0 || b_viewb < -318279180.0 ? 32.0 : b_viewb) % 1)), 1)
      break
   }
      b_viewb *= (Double(2 % (Swift.max(Int(b_viewb > 93607773.0 || b_viewb < -93607773.0 ? 25.0 : b_viewb), 8))))
     let messagesStorage: String! = String(cString: [108,97,109,101,0], encoding: .utf8)!
     let layerComments: [Any]! = [69, 48]
     let alphaAppearance: UIButton! = UIButton()
     let permissionsTip: UILabel! = UILabel(frame:CGRect.zero)
    var flagsRasterize = UIImageView()
    flagsRasterize.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    flagsRasterize.alpha = 0.4
    flagsRasterize.frame = CGRect(x: 243, y: 100, width: 0, height: 0)
    flagsRasterize.image = UIImage(named:String(cString: [119,95,116,105,116,108,101,0], encoding: .utf8)!)
    flagsRasterize.contentMode = .scaleAspectFit
    flagsRasterize.animationRepeatCount = 2
    alphaAppearance.alpha = 0.0;
    alphaAppearance.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    alphaAppearance.frame = CGRect(x: 48, y: 109, width: 0, height: 0)
    
    permissionsTip.alpha = 0.9;
    permissionsTip.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    permissionsTip.frame = CGRect(x: 6, y: 231, width: 0, height: 0)
    permissionsTip.textColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    permissionsTip.textAlignment = .right
    permissionsTip.font = UIFont.systemFont(ofSize:15)
    permissionsTip.text = ""
    

    
    return flagsRasterize

}






    @objc private func clickReverseButton() {

         let papersUndotted: UIImageView! = publishDisappearFollowLargeImageView(tabRoom:String(cString: [105,115,112,111,115,97,98,108,101,0], encoding: .utf8)!, existingFollowed:String(cString: [117,110,98,111,110,100,0], encoding: .utf8)!, cellType_f:[84, 93])

      if papersUndotted != nil {
          let papersUndotted_tag = papersUndotted.tag
          self.view.addSubview(papersUndotted)
      }

_ = papersUndotted


       var addF: String! = String(cString: [114,116,112,102,98,0], encoding: .utf8)!
    var sculptureo: [Any]! = [81, 12, 52]
   repeat {
       var cleanedb: Double = 2.0
      withUnsafeMutablePointer(to: &cleanedb) { pointer in
    
      }
       var peerx: [String: Any]! = [String(cString: [117,110,105,119,103,116,0], encoding: .utf8)!:25]
      withUnsafeMutablePointer(to: &peerx) { pointer in
             _ = pointer.pointee
      }
         cleanedb -= (Double(Int(cleanedb > 238797948.0 || cleanedb < -238797948.0 ? 5.0 : cleanedb) % (Swift.max(3, 2))))
         peerx = ["\(peerx.keys.count)": 3 << (Swift.min(5, peerx.keys.count))]
      for _ in 0 ..< 1 {
          var horizontale: String! = String(cString: [97,114,105,98,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &horizontale) { pointer in
    
         }
          var reverseT: Double = 2.0
          var deletedl: String! = String(cString: [102,114,101,101,112,0], encoding: .utf8)!
          var session9: [Any]! = [99.0]
         withUnsafeMutablePointer(to: &session9) { pointer in
    
         }
         cleanedb -= Double(peerx.keys.count)
         horizontale = "\((peerx.keys.count - Int(cleanedb > 198074063.0 || cleanedb < -198074063.0 ? 36.0 : cleanedb)))"
         reverseT -= Double(horizontale.count / (Swift.max(deletedl.count, 3)))
         deletedl = "\((Int(reverseT > 238921023.0 || reverseT < -238921023.0 ? 46.0 : reverseT)))"
         session9.append(horizontale.count)
      }
         peerx = ["\(peerx.count)": (3 | Int(cleanedb > 332536173.0 || cleanedb < -332536173.0 ? 93.0 : cleanedb))]
         cleanedb -= Double(peerx.keys.count)
          var statusI: String! = String(cString: [98,102,108,121,0], encoding: .utf8)!
         peerx = ["\(peerx.values.count)": statusI.count]
      addF = "\(sculptureo.count)"
      if addF.count == 2773259 {
         break
      }
   } while (addF.count == 2773259) && (!addF.contains("\(sculptureo.count)"))

        switchCamera()
    }

@discardableResult
 func supportRedPlayNameRangeActivityLabel(interval_xStyle: Float, cornerSend: Double) -> UILabel! {
    var t_titler: String! = String(cString: [102,97,115,116,109,97,116,104,0], encoding: .utf8)!
    var formatteri: Int = 2
   withUnsafeMutablePointer(to: &formatteri) { pointer in
          _ = pointer.pointee
   }
      formatteri &= 3 >> (Swift.min(1, t_titler.count))
      t_titler.append("\(t_titler.count & formatteri)")
     var stateDeleted: Int = 0
     let optionFans: Float = 46.0
     var currentTable: UILabel! = UILabel(frame:CGRect.zero)
    var bignumDiagnostic = UILabel()
    currentTable.alpha = 0.3;
    currentTable.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    currentTable.frame = CGRect(x: 88, y: 68, width: 0, height: 0)
    currentTable.font = UIFont.systemFont(ofSize:12)
    currentTable.text = ""
    currentTable.textColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    currentTable.textAlignment = .center
    
    bignumDiagnostic.text = ""
    bignumDiagnostic.textColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    bignumDiagnostic.textAlignment = .center
    bignumDiagnostic.font = UIFont.systemFont(ofSize:14)
    bignumDiagnostic.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    bignumDiagnostic.alpha = 0.3
    bignumDiagnostic.frame = CGRect(x: 195, y: 211, width: 0, height: 0)

    
    return bignumDiagnostic

}






    private func setupActions() {

         let decompressionXcorr: UILabel! = supportRedPlayNameRangeActivityLabel(interval_xStyle:19.0, cornerSend:39.0)

      if decompressionXcorr != nil {
          self.view.addSubview(decompressionXcorr)
          let decompressionXcorr_tag = decompressionXcorr.tag
      }

_ = decompressionXcorr


       var texte: Double = 3.0
   while ((texte / 5.4) < 3.40 || 2.38 < (texte / 5.4)) {
      texte -= Double(3)
      break
   }

        backButton.addTarget(self, action: #selector(clickBackButton), for: .touchUpInside)
        reverseButton.addTarget(self, action: #selector(clickReverseButton), for: .touchUpInside)
        cameraButton.addTarget(self, action: #selector(clickCameraButton), for: .touchUpInside)
        micButton.addTarget(self, action: #selector(clickMicButton), for: .touchUpInside)
        hangUpButton.addTarget(self, action: #selector(clickHangUpButton), for: .touchUpInside)
    }

@discardableResult
 func currentAtomicFormatAxisLayer(actionConfig: Float, discoverItem: Bool) -> [Any]! {
    var interval_f86: Int = 2
    var frame_1fV: String! = String(cString: [109,111,100,101,99,111,115,116,115,0], encoding: .utf8)!
    var hasL: [Any]! = [49, 83]
       var permissionsO: Double = 4.0
       _ = permissionsO
       var insetI: String! = String(cString: [103,101,116,108,97,121,111,117,116,0], encoding: .utf8)!
      if (Int(permissionsO > 221718019.0 || permissionsO < -221718019.0 ? 93.0 : permissionsO) * insetI.count) == 3 || 3.24 == (permissionsO * 5.79) {
          var indexo: String! = String(cString: [115,116,114,110,100,117,112,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &indexo) { pointer in
    
         }
         insetI = "\((Int(permissionsO > 392024730.0 || permissionsO < -392024730.0 ? 31.0 : permissionsO)))"
         indexo = "\(indexo.count)"
      }
       var hasa: [String: Any]! = [String(cString: [104,105,100,105,110,103,0], encoding: .utf8)!:56, String(cString: [109,115,109,112,101,103,100,97,116,97,0], encoding: .utf8)!:85, String(cString: [98,114,105,100,103,101,0], encoding: .utf8)!:35]
      withUnsafeMutablePointer(to: &hasa) { pointer in
    
      }
       var paintu: [String: Any]! = [String(cString: [105,110,118,101,114,118,97,108,0], encoding: .utf8)!:28.0]
       var gradientZ: Bool = true
       var placeholderH: Bool = false
      for _ in 0 ..< 2 {
         paintu = ["\(paintu.count)": 3 * paintu.values.count]
      }
       var settingP: [Any]! = [String(cString: [114,97,116,105,111,110,97,108,0], encoding: .utf8)!]
       var collection6: [Any]! = [74, 40]
       _ = collection6
      while (insetI.count < 1 && !placeholderH) {
         placeholderH = permissionsO > 28.20 && (String(cString:[115,0], encoding: .utf8)!) == insetI
         break
      }
         hasa["\(settingP.count)"] = hasa.values.count % 1
         gradientZ = settingP.count <= 26 && !gradientZ
         collection6.append(((placeholderH ? 4 : 1) - 1))
      hasL.append(hasL.count)
      frame_1fV = "\(2 & interval_f86)"
   return hasL

}






    override func viewDidLoad() {

         let sockNput: [Any]! = currentAtomicFormatAxisLayer(actionConfig:73.0, discoverItem:true)

      sockNput.enumerated().forEach({ (index,obj) in
          if index  <  93 {
                          print(obj)
          }
      })
      var sockNput_len = sockNput.count

_ = sockNput


       var authorL: String! = String(cString: [105,110,115,116,97,108,108,0], encoding: .utf8)!
       var cell2: Int = 5
      withUnsafeMutablePointer(to: &cell2) { pointer in
             _ = pointer.pointee
      }
       var lanK: Bool = false
       var coinsF: Float = 1.0
       var avatarsC: Float = 4.0
          var networkO: String! = String(cString: [108,105,102,101,99,121,99,108,101,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &networkO) { pointer in
                _ = pointer.pointee
         }
         cell2 &= (Int(coinsF > 144876247.0 || coinsF < -144876247.0 ? 14.0 : coinsF))
         networkO.append("\(((lanK ? 2 : 1)))")
       var stored3: Int = 0
      while (4 == (cell2 >> (Swift.min(labs(5), 3))) || lanK) {
         lanK = !lanK && 93.90 == coinsF
         break
      }
         cell2 /= Swift.max(4, ((lanK ? 2 : 3) | Int(coinsF > 316231545.0 || coinsF < -316231545.0 ? 86.0 : coinsF)))
      while ((coinsF + 5.89) < 4.19) {
         coinsF -= Float(1)
         break
      }
         avatarsC += Float(2)
         stored3 &= 2 / (Swift.max(stored3, 5))
      authorL = "\(cell2 + authorL.count)"

        super.viewDidLoad()

        backgroundView.isHidden = true
        view.backgroundColor = .black
        setupUI()
        setupActions()
        
        AH_RoomModity.shared.request(isShow: false) { _ in
        }
    }

@discardableResult
 func nowBasePopOutsideHomeImageView(dotRow: Bool, ageRegister_x7: Double, dimmingBlacklisted: Double) -> UIImageView! {
    var saveda: [String: Any]! = [String(cString: [103,97,109,109,97,118,97,108,0], encoding: .utf8)!:61, String(cString: [102,105,110,103,101,114,115,0], encoding: .utf8)!:47]
    var incomingp: String! = String(cString: [119,104,105,116,101,115,112,97,99,101,115,0], encoding: .utf8)!
      incomingp = "\(incomingp.count)"
      saveda[incomingp] = 1
     let long_evStored: Double = 50.0
     let storeImages: [Any]! = [String(cString: [115,117,112,101,114,0], encoding: .utf8)!, String(cString: [98,101,116,104,115,111,102,116,118,105,100,101,111,0], encoding: .utf8)!]
    var optionDiscontiguousPresence = UIImageView(frame:CGRect(x: 23, y: 81, width: 0, height: 0))
    optionDiscontiguousPresence.contentMode = .scaleAspectFit
    optionDiscontiguousPresence.animationRepeatCount = 6
    optionDiscontiguousPresence.image = UIImage(named:String(cString: [115,101,110,100,101,114,0], encoding: .utf8)!)
    optionDiscontiguousPresence.alpha = 0.3;
    optionDiscontiguousPresence.backgroundColor = UIColor(red:0, green:0, blue:1, alpha: 1)
    optionDiscontiguousPresence.frame = CGRect(x: 199, y: 50, width: 0, height: 0)

    
    return optionDiscontiguousPresence

}






    override func viewWillDisappear(_ animated: Bool) {

         var blankBetter: UIImageView! = nowBasePopOutsideHomeImageView(dotRow:true, ageRegister_x7:35.0, dimmingBlacklisted:78.0)

      if blankBetter != nil {
          let blankBetter_tag = blankBetter.tag
          self.view.addSubview(blankBetter)
      }

withUnsafeMutablePointer(to: &blankBetter) { pointer in
    
}


       var press2: String! = String(cString: [116,114,97,110,115,108,105,116,101,114,97,116,105,111,110,0], encoding: .utf8)!
    var gradientk: Float = 3.0
    _ = gradientk
      gradientk += (Float(press2.count | Int(gradientk > 306503643.0 || gradientk < -306503643.0 ? 17.0 : gradientk)))

   if (press2.count ^ 5) == 5 {
      press2 = "\((2 | Int(gradientk > 23450171.0 || gradientk < -23450171.0 ? 47.0 : gradientk)))"
   }
        super.viewWillDisappear(animated)
        if isMovingFromParent {
            stopCameraSession()
        }
    }

@discardableResult
 func hangLargeApplyAngleDissolveBlueButton(createdReverse: Double, agreementBubble: [Any]!, storedLabel: [Any]!) -> UIButton! {
    var catalogb: Double = 5.0
    _ = catalogb
    var introc: Double = 4.0
      catalogb /= Swift.max(1, (Double(1 * Int(catalogb > 19278425.0 || catalogb < -19278425.0 ? 64.0 : catalogb))))
   repeat {
      introc /= Swift.max(3, (Double(Int(catalogb > 130830060.0 || catalogb < -130830060.0 ? 8.0 : catalogb))))
      if introc == 4139693.0 {
         break
      }
   } while (introc > catalogb) && (introc == 4139693.0)
     var answerCell: Double = 60.0
     let enew_kwPassword: UILabel! = UILabel()
     let followingLan: Int = 28
     var policyFollowing: UILabel! = UILabel()
    var deadSwirlExif:UIButton! = UIButton(frame:CGRect(x: 161, y: 26, width: 0, height: 0))
    deadSwirlExif.titleLabel?.font = UIFont.systemFont(ofSize:15)
    deadSwirlExif.setImage(UIImage(named:String(cString: [102,97,110,115,0], encoding: .utf8)!), for: .normal)
    deadSwirlExif.setTitle("", for: .normal)
    deadSwirlExif.setBackgroundImage(UIImage(named:String(cString: [113,95,112,111,115,105,116,105,111,110,0], encoding: .utf8)!), for: .normal)
    deadSwirlExif.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    deadSwirlExif.alpha = 0.4
    deadSwirlExif.frame = CGRect(x: 310, y: 122, width: 0, height: 0)
    enew_kwPassword.frame = CGRect(x: 69, y: 11, width: 0, height: 0)
    enew_kwPassword.alpha = 0.6;
    enew_kwPassword.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    enew_kwPassword.textColor = UIColor(red:0, green:0, blue:1, alpha: 0)
    enew_kwPassword.textAlignment = .center
    enew_kwPassword.font = UIFont.systemFont(ofSize:20)
    enew_kwPassword.text = ""
    
    policyFollowing.alpha = 0.8;
    policyFollowing.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    policyFollowing.frame = CGRect(x: 23, y: 187, width: 0, height: 0)
    policyFollowing.text = ""
    policyFollowing.textColor = UIColor(red:0, green:0, blue:0, alpha: 1)
    policyFollowing.textAlignment = .left
    policyFollowing.font = UIFont.systemFont(ofSize:13)
    

    
    return deadSwirlExif

}






    private func startCameraSession() {

         var inclusionsLoopbreak: UIButton! = hangLargeApplyAngleDissolveBlueButton(createdReverse:98.0, agreementBubble:[false], storedLabel:[100, 56])

      if inclusionsLoopbreak != nil {
          let inclusionsLoopbreak_tag = inclusionsLoopbreak.tag
          self.view.addSubview(inclusionsLoopbreak)
      }

withUnsafeMutablePointer(to: &inclusionsLoopbreak) { pointer in
    
}


       var startU: String! = String(cString: [105,110,116,101,114,105,111,114,0], encoding: .utf8)!
      startU = "\(3)"

        guard captureSession == nil else {
            if isCameraEnabled {
                DispatchQueue.global(qos: .userInitiated).async { [weak self] in
                    self?.captureSession?.startRunning()
                }
            }
            return
        }

        let provider = AVCaptureSession()
        provider.sessionPreset = .high

        guard
            let overlay = cameraDevice(for: currentCameraPosition),
            let privacy = try? AVCaptureDeviceInput(device: overlay),
            provider.canAddInput(privacy)
        else {
            return
        }

        provider.addInput(privacy)
        videoInput = privacy
        captureSession = provider

        let pending = AVCaptureVideoPreviewLayer(session: provider)
        pending.videoGravity = .resizeAspectFill
        pending.frame = previewView.bounds
        previewView.layer.insertSublayer(pending, at: 0)
        previewLayer = pending

        DispatchQueue.global(qos: .userInitiated).async {
            provider.startRunning()
        }
    }

@discardableResult
 func underlineEachState(reviseItem: [String: Any]!, historyNews: Int, barLogged: String!) -> Double {
    var publishj: String! = String(cString: [114,101,108,97,116,105,118,101,108,121,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &publishj) { pointer in
          _ = pointer.pointee
   }
    var datae: String! = String(cString: [115,117,112,112,114,101,115,115,111,114,0], encoding: .utf8)!
   repeat {
       var tabI: String! = String(cString: [112,114,101,100,105,99,116,105,111,110,115,0], encoding: .utf8)!
       var peerT: String! = String(cString: [112,97,114,107,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &peerT) { pointer in
             _ = pointer.pointee
      }
       var action6: String! = String(cString: [103,114,97,121,114,103,98,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &action6) { pointer in
             _ = pointer.pointee
      }
       var itemQ: String! = String(cString: [120,99,111,100,101,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &itemQ) { pointer in
             _ = pointer.pointee
      }
      for _ in 0 ..< 3 {
          var dynamic_ju: String! = String(cString: [114,105,110,103,116,111,110,101,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &dynamic_ju) { pointer in
                _ = pointer.pointee
         }
          var saveds: String! = String(cString: [111,109,112,108,105,99,97,116,105,111,110,0], encoding: .utf8)!
         peerT.append("\(2 & action6.count)")
         dynamic_ju.append("\(action6.count ^ saveds.count)")
         saveds = "\(3 % (Swift.max(9, action6.count)))"
      }
         peerT = "\(((String(cString:[109,0], encoding: .utf8)!) == itemQ ? action6.count : itemQ.count))"
       var likeq: String! = String(cString: [105,110,105,116,118,0], encoding: .utf8)!
       var anchorM: String! = String(cString: [105,112,99,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &anchorM) { pointer in
             _ = pointer.pointee
      }
       var postG: String! = String(cString: [99,108,111,99,107,100,114,105,102,116,0], encoding: .utf8)!
       _ = postG
       var enews1: String! = String(cString: [108,101,97,118,105,110,103,0], encoding: .utf8)!
       _ = enews1
      if postG != likeq {
         likeq = "\(((String(cString:[99,0], encoding: .utf8)!) == enews1 ? enews1.count : peerT.count))"
      }
          var discoverl: [Any]! = [String(cString: [114,108,105,110,101,0], encoding: .utf8)!, String(cString: [100,105,103,101,115,116,115,0], encoding: .utf8)!, String(cString: [97,117,116,111,99,111,114,114,101,99,116,105,111,110,0], encoding: .utf8)!]
          var fullE: String! = String(cString: [115,99,114,101,97,109,0], encoding: .utf8)!
          var emailv: String! = String(cString: [111,108,97,110,97,0], encoding: .utf8)!
          _ = emailv
         itemQ.append("\(emailv.count)")
         discoverl.append(itemQ.count)
         fullE.append("\(fullE.count * itemQ.count)")
      repeat {
         likeq = "\(peerT.count ^ 3)"
         if (String(cString:[52,54,99,0], encoding: .utf8)!) == likeq {
            break
         }
      } while ((String(cString:[52,54,99,0], encoding: .utf8)!) == likeq) && (anchorM == likeq)
         itemQ = "\(anchorM.count + postG.count)"
      publishj = "\(tabI.count & datae.count)"
      if publishj == (String(cString:[108,110,120,112,100,118,102,118,100,0], encoding: .utf8)!) {
         break
      }
   } while (datae.hasSuffix(publishj)) && (publishj == (String(cString:[108,110,120,112,100,118,102,118,100,0], encoding: .utf8)!))
      datae = "\(1)"
     var attachmentUpdates: String! = String(cString: [114,116,115,112,99,111,100,101,115,0], encoding: .utf8)!
     let selPrice: String! = String(cString: [98,101,110,99,104,0], encoding: .utf8)!
     let hnew__Tab: String! = String(cString: [104,97,108,102,102,108,111,97,116,0], encoding: .utf8)!
     var layoutAttachment: String! = String(cString: [115,97,118,101,100,0], encoding: .utf8)!
    var shutdownackWdec:Double = 0

    return shutdownackWdec

}






    @objc private func clickBackButton() {

         let tickingNavi: Double = underlineEachState(reviseItem:[String(cString: [99,108,109,117,108,0], encoding: .utf8)!:61, String(cString: [105,110,103,114,101,115,115,0], encoding: .utf8)!:68], historyNews:20, barLogged:String(cString: [117,110,99,97,99,104,101,100,0], encoding: .utf8)!)

      if tickingNavi < 62 {
             print(tickingNavi)
      }

_ = tickingNavi


       var description_0T: Int = 1
    _ = description_0T
      description_0T >>= Swift.min(labs(3 | description_0T), 3)

        navigationController?.popViewController(animated: true)
    }

@discardableResult
 func backgroundSaveResumeCollectionInside(attachmentLast: Bool) -> [Any]! {
    var dimmingH: Double = 4.0
    _ = dimmingH
    var intro1: Double = 4.0
    var authorl: [Any]! = [49, 13, 42]
   withUnsafeMutablePointer(to: &authorl) { pointer in
          _ = pointer.pointee
   }
   while (4.94 < (dimmingH + intro1)) {
      intro1 -= (Double(Int(dimmingH > 57556492.0 || dimmingH < -57556492.0 ? 13.0 : dimmingH)))
      break
   }
      intro1 /= Swift.max(4, (Double(2 >> (Swift.min(labs(Int(intro1 > 387231177.0 || intro1 < -387231177.0 ? 36.0 : intro1)), 1)))))
   for _ in 0 ..< 1 {
      authorl.append((Int(intro1 > 382012336.0 || intro1 < -382012336.0 ? 46.0 : intro1)))
   }
   return authorl

}






    @objc private func clickMicButton() {

         var iqmpNellymoser: [Any]! = backgroundSaveResumeCollectionInside(attachmentLast:false)

      let iqmpNellymoser_len = iqmpNellymoser.count
      iqmpNellymoser.forEach({ (obj) in
          print(obj)
      })

withUnsafeMutablePointer(to: &iqmpNellymoser) { pointer in
        _ = pointer.pointee
}


       var alertI: Int = 0
      alertI ^= alertI

        isMicEnabled.toggle()
        updateControlButtons()
    }


    override func viewDidAppear(_ animated: Bool) {
       var interval_lH: Float = 4.0
    var dotO: [String: Any]! = [String(cString: [109,97,103,105,99,121,117,118,0], encoding: .utf8)!:String(cString: [105,110,99,114,101,109,101,110,116,115,0], encoding: .utf8)!, String(cString: [112,114,111,98,101,114,0], encoding: .utf8)!:String(cString: [114,101,102,100,117,112,101,0], encoding: .utf8)!, String(cString: [112,114,101,109,105,117,109,0], encoding: .utf8)!:String(cString: [100,105,115,101,109,118,111,119,101,108,101,100,0], encoding: .utf8)!]
    _ = dotO
   repeat {
       var errorR: Double = 2.0
       var barN: Float = 4.0
       var m_imagew: String! = String(cString: [100,117,109,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &m_imagew) { pointer in
             _ = pointer.pointee
      }
       var filter_: [Any]! = [String(cString: [103,108,97,115,115,0], encoding: .utf8)!, String(cString: [109,97,116,99,104,105,110,102,111,0], encoding: .utf8)!, String(cString: [114,101,115,105,100,117,101,0], encoding: .utf8)!]
         m_imagew = "\(((String(cString:[88,0], encoding: .utf8)!) == m_imagew ? Int(errorR > 179808788.0 || errorR < -179808788.0 ? 76.0 : errorR) : m_imagew.count))"
          var actionQ: Int = 4
          var resourcec: Bool = false
         barN -= (Float((resourcec ? 1 : 2) / (Swift.max(Int(errorR > 261134337.0 || errorR < -261134337.0 ? 30.0 : errorR), 4))))
         actionQ &= 3 / (Swift.max(4, m_imagew.count))
       var mediaD: String! = String(cString: [115,111,114,101,99,101,105,118,101,0], encoding: .utf8)!
       var responset: String! = String(cString: [105,110,105,116,105,97,116,101,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &responset) { pointer in
    
      }
      repeat {
         m_imagew.append("\((Int(barN > 284456638.0 || barN < -284456638.0 ? 76.0 : barN)))")
         if (String(cString:[48,114,119,0], encoding: .utf8)!) == m_imagew {
            break
         }
      } while ((String(cString:[48,114,119,0], encoding: .utf8)!) == m_imagew) && ((errorR * 4.24) >= 2.62)
      while (2 >= (filter_.count + 1)) {
         errorR -= (Double(Int(barN > 9839747.0 || barN < -9839747.0 ? 52.0 : barN) * 1))
         break
      }
         responset.append("\(3)")
         errorR += (Double(Int(barN > 347383008.0 || barN < -347383008.0 ? 90.0 : barN) * Int(errorR > 130590501.0 || errorR < -130590501.0 ? 89.0 : errorR)))
         mediaD = "\(mediaD.count)"
         m_imagew = "\(2 >> (Swift.min(1, mediaD.count)))"
      if barN <= 5.51 {
         errorR -= (Double((String(cString:[70,0], encoding: .utf8)!) == m_imagew ? m_imagew.count : Int(barN > 154963281.0 || barN < -154963281.0 ? 33.0 : barN)))
      }
      while (4.12 > (Double(3 * Int(barN)))) {
         errorR -= Double(m_imagew.count * filter_.count)
         break
      }
         errorR -= (Double((String(cString:[98,0], encoding: .utf8)!) == mediaD ? responset.count : mediaD.count))
      dotO["\(errorR)"] = 1
      if dotO.count == 493765 {
         break
      }
   } while (1 < (1 | dotO.keys.count) && 4.33 < (interval_lH / (Swift.max(Float(dotO.keys.count), 4)))) && (dotO.count == 493765)

   for _ in 0 ..< 3 {
       var scrollf: Bool = true
       var followedJ: Bool = false
       var responseT: String! = String(cString: [116,109,109,98,114,0], encoding: .utf8)!
       _ = responseT
       var bubble0: String! = String(cString: [105,110,115,116,114,0], encoding: .utf8)!
       _ = bubble0
       var controllero: String! = String(cString: [104,100,110,111,100,101,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &controllero) { pointer in
    
      }
      repeat {
         scrollf = (((scrollf ? responseT.count : 10) << (Swift.min(responseT.count, 4))) > 10)
         if scrollf ? !scrollf : scrollf {
            break
         }
      } while (scrollf ? !scrollf : scrollf) && (!followedJ)
          var optionH: Double = 0.0
          var tipO: [Any]! = [92, 34]
          var time_uK: Bool = false
         scrollf = (bubble0.count + tipO.count) == 35
         optionH += Double(2)
         time_uK = 13 >= controllero.count && (String(cString:[75,0], encoding: .utf8)!) == responseT
      if responseT.hasPrefix("\(followedJ)") {
         followedJ = responseT.count == 9 && followedJ
      }
       var applew: [Any]! = [34, 60]
       _ = applew
       var fieldw: [Any]! = [true]
       var paddingV: [String: Any]! = [String(cString: [100,101,108,97,117,110,97,121,0], encoding: .utf8)!:67, String(cString: [112,111,112,0], encoding: .utf8)!:63]
       var fileX: [String: Any]! = [String(cString: [104,97,115,104,101,115,0], encoding: .utf8)!:43, String(cString: [115,97,110,105,116,105,122,101,114,0], encoding: .utf8)!:58, String(cString: [98,117,103,115,0], encoding: .utf8)!:6]
       _ = fileX
      if 4 < (fileX.count + 2) {
          var authorizationL: String! = String(cString: [115,121,110,99,109,97,114,107,101,114,0], encoding: .utf8)!
          _ = authorizationL
          var update_fo3: String! = String(cString: [117,110,115,97,118,101,100,0], encoding: .utf8)!
         fileX = [responseT: 2]
         authorizationL = "\(((scrollf ? 3 : 4) + controllero.count))"
         update_fo3 = "\(1 * bubble0.count)"
      }
         bubble0 = "\(3 >> (Swift.min(1, bubble0.count)))"
         controllero = "\(2)"
       var pressF: Bool = false
       _ = pressF
         paddingV = ["\(paddingV.keys.count)": applew.count]
      repeat {
          var empty_: String! = String(cString: [101,110,100,101,100,0], encoding: .utf8)!
          var type_5h: String! = String(cString: [97,99,99,101,108,101,114,97,116,101,100,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &type_5h) { pointer in
    
         }
          var enabledn: Float = 3.0
          var controlg: [String: Any]! = [String(cString: [102,97,97,110,100,99,116,0], encoding: .utf8)!:41, String(cString: [97,116,97,98,108,101,115,0], encoding: .utf8)!:45, String(cString: [102,114,97,109,101,105,110,102,111,0], encoding: .utf8)!:67]
         fileX["\(followedJ)"] = (1 * (followedJ ? 3 : 5))
         empty_.append("\(responseT.count % 2)")
         type_5h.append("\(applew.count)")
         enabledn += Float(controlg.values.count)
         controlg = [responseT: responseT.count]
         if fileX.count == 2487802 {
            break
         }
      } while ((3 >> (Swift.min(5, fileX.values.count))) <= 2 || (paddingV.count >> (Swift.min(labs(3), 5))) <= 3) && (fileX.count == 2487802)
         fileX["\(fieldw.count)"] = paddingV.values.count | fieldw.count
         scrollf = 15 == applew.count || responseT == (String(cString:[111,0], encoding: .utf8)!)
          var loginx: String! = String(cString: [104,97,114,109,111,110,105,99,0], encoding: .utf8)!
          _ = loginx
          var tabC: String! = String(cString: [110,101,115,116,0], encoding: .utf8)!
         scrollf = !responseT.hasSuffix("\(pressF)")
         loginx = "\(fieldw.count & 1)"
         tabC.append("\(2)")
      repeat {
         pressF = paddingV.keys.count < 7
         if pressF ? !pressF : pressF {
            break
         }
      } while (!pressF) && (pressF ? !pressF : pressF)
      interval_lH /= Swift.max(Float(2), 5)
   }
        super.viewDidAppear(animated)
        guard !hasCheckedPermissions else { return }
        hasCheckedPermissions = true
        checkMediaPermissions()
    }

@discardableResult
 func keyboardPlainRect(loggedStore: Double, settingMode: Float) -> Int {
    var black2: String! = String(cString: [99,111,109,112,114,101,115,115,0], encoding: .utf8)!
    var attributes5: Double = 4.0
    var relations8: Int = 5
    _ = relations8
      black2 = "\(black2.count)"
      relations8 |= (Int(attributes5 > 270936556.0 || attributes5 < -270936556.0 ? 84.0 : attributes5) | black2.count)
      attributes5 += (Double(Int(attributes5 > 17553839.0 || attributes5 < -17553839.0 ? 63.0 : attributes5)))
   return relations8

}






    private func showPermissionAlert() {

         let avprivDtmf: Int = keyboardPlainRect(loggedStore:20.0, settingMode:43.0)

      if avprivDtmf <= 85 {
             print(avprivDtmf)
      }

_ = avprivDtmf


       var appn: String! = String(cString: [97,117,116,111,109,97,116,105,99,0], encoding: .utf8)!
    var extension_5cK: Float = 1.0
      extension_5cK -= (Float((String(cString:[104,0], encoding: .utf8)!) == appn ? Int(extension_5cK > 151365122.0 || extension_5cK < -151365122.0 ? 12.0 : extension_5cK) : appn.count))

   repeat {
       var peers: String! = String(cString: [99,111,108,100,0], encoding: .utf8)!
       var d_playerq: [String: Any]! = [String(cString: [99,108,97,115,115,105,102,105,101,114,0], encoding: .utf8)!:[78, 96]]
         d_playerq[peers] = 2
         peers = "\(((String(cString:[105,0], encoding: .utf8)!) == peers ? d_playerq.count : peers.count))"
      for _ in 0 ..< 3 {
         peers = "\(3)"
      }
          var default_b44: [String: Any]! = [String(cString: [112,97,99,107,101,116,0], encoding: .utf8)!:44.0]
          _ = default_b44
          var unread6: String! = String(cString: [114,101,99,111,114,100,115,0], encoding: .utf8)!
         peers = "\(d_playerq.values.count % (Swift.max(3, 9)))"
         default_b44 = [unread6: 2 >> (Swift.min(2, unread6.count))]
      repeat {
         d_playerq[peers] = (peers == (String(cString:[108,0], encoding: .utf8)!) ? peers.count : d_playerq.keys.count)
         if d_playerq.count == 1491590 {
            break
         }
      } while (d_playerq.count == 1491590) && (4 <= (peers.count | d_playerq.count))
         peers.append("\(peers.count)")
      appn = "\(((String(cString:[89,0], encoding: .utf8)!) == appn ? appn.count : Int(extension_5cK > 92291773.0 || extension_5cK < -92291773.0 ? 58.0 : extension_5cK)))"
      if appn == (String(cString:[54,101,113,52,0], encoding: .utf8)!) {
         break
      }
   } while (4 > (2 - Int(extension_5cK > 29854905.0 || extension_5cK < -29854905.0 ? 85.0 : extension_5cK)) && 2 > (Int(extension_5cK > 64223079.0 || extension_5cK < -64223079.0 ? 9.0 : extension_5cK) - appn.count)) && (appn == (String(cString:[54,101,113,52,0], encoding: .utf8)!))
        let network = UIAlertController(
            title: "Permissions Required",
            message: "Please enable camera and microphone access in Settings to start a video call.",
            preferredStyle: .alert
        )
        network.addAction(UIAlertAction(title: "Cancel", style: .cancel) { [weak self] _ in
            self?.navigationController?.popViewController(animated: true)
        })
        network.addAction(UIAlertAction(title: "Settings", style: .default) { _ in
            guard let url = URL(string: UIApplication.openSettingsURLString) else { return }
            UIApplication.shared.open(url)
        })
        present(network, animated: true)
    }


    private func ensurePermission(for mediaType: AVMediaType, completion: @escaping (Bool) -> Void) {
       var componentsH: Double = 1.0
   repeat {
      componentsH += (Double(1 / (Swift.max(10, Int(componentsH > 119694860.0 || componentsH < -119694860.0 ? 65.0 : componentsH)))))
      if componentsH == 1912309.0 {
         break
      }
   } while ((5.84 / (Swift.max(7, componentsH))) == 5.97) && (componentsH == 1912309.0)

        let request7: AVAuthorizationStatus
        if mediaType == .video {
            request7 = AVCaptureDevice.authorizationStatus(for: .video)
        } else {
            request7 = AVCaptureDevice.authorizationStatus(for: .audio)
        }

        switch request7 {
        case .authorized:
            completion(true)
        case .notDetermined:
            if mediaType == .video {
                AVCaptureDevice.requestAccess(for: .video, completionHandler: completion)
            } else {
                AVCaptureDevice.requestAccess(for: .audio, completionHandler: completion)
            }
        default:
            completion(false)
        }
    }


    private func setupUI() {
       var createt: Double = 5.0
   for _ in 0 ..< 3 {
       var errorv: Int = 1
       _ = errorv
       var listE: Double = 4.0
       _ = listE
       var adde: [Any]! = [UILabel(frame:CGRect(x: 174, y: 145, width: 0, height: 0))]
       _ = adde
       var reviseG: String! = String(cString: [98,105,116,0], encoding: .utf8)!
       var policyT: String! = String(cString: [97,114,114,97,110,103,101,100,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &policyT) { pointer in
    
      }
      if adde.count == 3 {
         adde.append(errorv ^ 2)
      }
      repeat {
         adde.append(adde.count - reviseG.count)
         if adde.count == 717751 {
            break
         }
      } while (3 > (errorv % (Swift.max(1, 8))) && (errorv % (Swift.max(4, adde.count))) > 1) && (adde.count == 717751)
      if 1.4 >= (2.2 * listE) && (listE * Double(errorv)) >= 2.2 {
         errorv %= Swift.max(2, reviseG.count)
      }
       var avatarsr: String! = String(cString: [116,105,109,101,119,97,105,116,0], encoding: .utf8)!
      repeat {
         policyT.append("\(3 + errorv)")
         if (String(cString:[111,100,50,106,0], encoding: .utf8)!) == policyT {
            break
         }
      } while (policyT.count == 2) && ((String(cString:[111,100,50,106,0], encoding: .utf8)!) == policyT)
      for _ in 0 ..< 2 {
          var existingE: String! = String(cString: [105,110,116,114,97,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &existingE) { pointer in
    
         }
         errorv >>= Swift.min(2, labs(existingE.count >> (Swift.min(1, avatarsr.count))))
      }
          var blacklistedT: Float = 4.0
         avatarsr = "\(reviseG.count)"
         blacklistedT /= Swift.max(1, (Float(Int(blacklistedT > 233599372.0 || blacklistedT < -233599372.0 ? 77.0 : blacklistedT))))
      if 3 < reviseG.count || avatarsr == String(cString:[73,0], encoding: .utf8)! {
          var result4: Double = 1.0
         reviseG = "\(adde.count ^ 2)"
         result4 /= Swift.max(Double(1), 4)
      }
      while (!adde.contains { $0 as? Int == errorv }) {
         adde = [avatarsr.count]
         break
      }
      for _ in 0 ..< 1 {
         errorv -= adde.count
      }
       var blueF: Bool = false
      withUnsafeMutablePointer(to: &blueF) { pointer in
    
      }
          var long__I: Float = 1.0
         withUnsafeMutablePointer(to: &long__I) { pointer in
    
         }
          var mnew_vt: Int = 5
          var sculpturep: String! = String(cString: [100,105,102,102,101,114,101,110,99,101,0], encoding: .utf8)!
         policyT = "\(adde.count ^ 1)"
         long__I -= Float(1 & mnew_vt)
         mnew_vt /= Swift.max(3, 1 - adde.count)
         sculpturep.append("\(adde.count >> (Swift.min(labs(3), 4)))")
          var window_th: Double = 3.0
          var allJ: String! = String(cString: [114,101,106,101,99,116,105,111,110,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &allJ) { pointer in
    
         }
         avatarsr = "\(((String(cString:[83,0], encoding: .utf8)!) == policyT ? policyT.count : avatarsr.count))"
         window_th -= Double(2)
         allJ = "\(2)"
         policyT.append("\(errorv + avatarsr.count)")
         blueF = listE > 1.67
      createt += Double(adde.count)
   }

        view.addSubview(previewView)
        view.addSubview(backButton)
        view.addSubview(peerContainerView)
        peerContainerView.addSubview(peerAvatarView)
        peerContainerView.addSubview(loadingIndicator)
        view.addSubview(bottomPanelView)
        bottomPanelView.addSubview(controlStackView)

        controlStackView.addArrangedSubview(reverseButton)
        controlStackView.addArrangedSubview(cameraButton)
        controlStackView.addArrangedSubview(micButton)
        controlStackView.addArrangedSubview(hangUpButton)

        previewView.snp.makeConstraints { make in
            make.edges.equalToSuperview()
        }
        backButton.snp.makeConstraints { make in
            make.leading.equalToSuperview().offset(6)
            make.top.equalTo(view.safeAreaLayoutGuide)
            make.size.equalTo(44)
        }
        peerContainerView.snp.makeConstraints { make in
            make.trailing.equalToSuperview().offset(-16)
            make.top.equalTo(view.safeAreaLayoutGuide).offset(8)
            make.width.equalTo(110)
            make.height.equalTo(150)
        }
        peerAvatarView.snp.makeConstraints { make in
            make.edges.equalToSuperview()
        }
        loadingIndicator.snp.makeConstraints { make in
            make.center.equalToSuperview()
        }
        bottomPanelView.snp.makeConstraints { make in
            make.leading.trailing.bottom.equalToSuperview()
        }
        controlStackView.snp.makeConstraints { make in
            make.leading.trailing.equalToSuperview().inset(24)
            make.top.equalToSuperview().offset(24)
            make.bottom.equalTo(view.safeAreaLayoutGuide).offset(-24)
            make.height.equalTo(72)
        }

        [reverseButton, cameraButton, micButton, hangUpButton].forEach { button in
            button.snp.makeConstraints { make in
                make.size.equalTo(72)
            }
        }

        peerAvatarView.image = peerAvatarImage
        loadingIndicator.startAnimating()
        updateControlButtons()
    }

@discardableResult
 func confirmHomeBounce(answersGreen: Double) -> [Any]! {
    var filterw: Float = 1.0
    var makeY: Bool = false
    var bottomf: [Any]! = [false]
   if !makeY {
       var modelt: String! = String(cString: [115,116,111,114,97,98,108,101,0], encoding: .utf8)!
       var homez: [Any]! = [UILabel()]
       var cameram: String! = String(cString: [97,99,115,107,105,112,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &cameram) { pointer in
             _ = pointer.pointee
      }
       var greend: Double = 5.0
       var allC: Int = 2
         homez.append(1)
      for _ in 0 ..< 3 {
         modelt = "\((cameram == (String(cString:[68,0], encoding: .utf8)!) ? cameram.count : allC))"
      }
      repeat {
         modelt = "\(cameram.count)"
         if 575517 == modelt.count {
            break
         }
      } while (575517 == modelt.count) && ((5 >> (Swift.min(3, modelt.count))) > 4 && 5 > (modelt.count >> (Swift.min(1, homez.count))))
         greend -= (Double(1 + Int(greend > 282703416.0 || greend < -282703416.0 ? 55.0 : greend)))
         allC %= Swift.max(3 % (Swift.max(7, homez.count)), 4)
         greend -= (Double(cameram == (String(cString:[86,0], encoding: .utf8)!) ? allC : cameram.count))
      repeat {
         homez.append(modelt.count)
         if 2162597 == homez.count {
            break
         }
      } while (!cameram.contains("\(homez.count)")) && (2162597 == homez.count)
      if (4 + Int(greend > 272591387.0 || greend < -272591387.0 ? 9.0 : greend)) <= 3 && 3.9 <= (greend + 4.78) {
         cameram.append("\(1)")
      }
         homez.append(cameram.count ^ modelt.count)
         modelt = "\(modelt.count / (Swift.max(10, cameram.count)))"
      if 4 <= (allC - 1) && 1 <= (homez.count - allC) {
         homez.append((homez.count * Int(greend > 273874742.0 || greend < -273874742.0 ? 90.0 : greend)))
      }
      if 2 > (Int(greend > 148647465.0 || greend < -148647465.0 ? 40.0 : greend) + cameram.count) && 5.6 > (greend + 4.90) {
         cameram = "\((cameram == (String(cString:[87,0], encoding: .utf8)!) ? Int(greend > 100185749.0 || greend < -100185749.0 ? 95.0 : greend) : cameram.count))"
      }
         modelt = "\(homez.count * modelt.count)"
         greend -= Double(1)
      if 5 > (allC & 2) {
         allC += allC
      }
      makeY = 20 <= allC || bottomf.count <= 20
   }
   for _ in 0 ..< 2 {
      makeY = 19.16 > filterw
   }
   for _ in 0 ..< 3 {
      bottomf = [((makeY ? 1 : 2) % (Swift.max(bottomf.count, 9)))]
   }
   return bottomf

}






    private func updateControlButtons() {

         var segfeaturesToolbox: [Any]! = confirmHomeBounce(answersGreen:47.0)

      let segfeaturesToolbox_len = segfeaturesToolbox.count
      segfeaturesToolbox.enumerated().forEach({ (index,obj) in
          if index  <=  0 {
                print(obj)
          }
      })

withUnsafeMutablePointer(to: &segfeaturesToolbox) { pointer in
        _ = pointer.pointee
}


       var photosb: String! = String(cString: [100,114,105,118,105,110,103,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &photosb) { pointer in
    
   }
   repeat {
      photosb = "\(3 + photosb.count)"
      if photosb == (String(cString:[119,105,122,48,0], encoding: .utf8)!) {
         break
      }
   } while (photosb == (String(cString:[119,105,122,48,0], encoding: .utf8)!)) && (1 > photosb.count)

        let x_layer = isCameraEnabled ? "room_camera" : "room_camera_off"
        let task = isMicEnabled ? "room_mic" : "room_mic_off"
        cameraButton.setImage(UIImage(named: x_layer), for: .normal)
        micButton.setImage(UIImage(named: task), for: .normal)
        previewView.isHidden = !isCameraEnabled
    }

@discardableResult
 func rowStringAppleDismissThan(resultPosts: Double, actionSession: String!, appearanceExtension_w: Double) -> String! {
    var date8: String! = String(cString: [110,101,111,110,0], encoding: .utf8)!
    var builtS: Double = 0.0
    var barX: String! = String(cString: [104,97,115,0], encoding: .utf8)!
       var main_af: Int = 0
          var dock: [Any]! = [73, 11]
         main_af -= dock.count ^ 3
         main_af ^= main_af
      repeat {
         main_af |= main_af
         if 1940969 == main_af {
            break
         }
      } while (1940969 == main_af) && (5 == (main_af % 2))
      barX = "\((3 ^ Int(builtS > 171527949.0 || builtS < -171527949.0 ? 2.0 : builtS)))"
      builtS -= (Double(Int(builtS > 268519981.0 || builtS < -268519981.0 ? 92.0 : builtS)))
   if (barX.count ^ 4) == 3 {
      builtS /= Swift.max(4, (Double(Int(builtS > 152793409.0 || builtS < -152793409.0 ? 29.0 : builtS))))
   }
      date8.append("\(date8.count)")
   return date8

}






    @objc private func clickHangUpButton() {

         var ownershipComparison: String! = rowStringAppleDismissThan(resultPosts:50.0, actionSession:String(cString: [99,114,111,115,115,0], encoding: .utf8)!, appearanceExtension_w:88.0)

      let ownershipComparison_len = ownershipComparison?.count ?? 0
      print(ownershipComparison)

withUnsafeMutablePointer(to: &ownershipComparison) { pointer in
        _ = pointer.pointee
}


       var attributed_: Int = 4
   while (4 < attributed_) {
      attributed_ += 1
      break
   }

        navigationController?.popViewController(animated: true)
    }


    override func viewDidLayoutSubviews() {
       var currentp: String! = String(cString: [98,103,114,97,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &currentp) { pointer in
          _ = pointer.pointee
   }
    var valuef: Double = 0.0
      currentp = "\(((String(cString:[65,0], encoding: .utf8)!) == currentp ? currentp.count : Int(valuef > 294658120.0 || valuef < -294658120.0 ? 6.0 : valuef)))"
      currentp.append("\(2)")

   if 3.35 < (valuef / (Swift.max(valuef, 7))) {
      valuef /= Swift.max((Double(Int(valuef > 212695692.0 || valuef < -212695692.0 ? 26.0 : valuef))), 3)
   }
        super.viewDidLayoutSubviews()
        previewLayer?.frame = previewView.bounds
    }

@discardableResult
 func rawAppearUnknownData(postsResult: Float) -> Bool {
    var lastE: Double = 0.0
    var cornerM: Double = 2.0
   withUnsafeMutablePointer(to: &cornerM) { pointer in
    
   }
    var buyq: Bool = true
   repeat {
      buyq = 7.11 > lastE && 7.11 > cornerM
      if buyq ? !buyq : buyq {
         break
      }
   } while (buyq ? !buyq : buyq) && ((4.100 - lastE) > 4.22 && buyq)
   while ((lastE / (Swift.max(6, cornerM))) < 1.4) {
      cornerM -= (Double(Int(cornerM > 260443041.0 || cornerM < -260443041.0 ? 3.0 : cornerM) | 3))
      break
   }
      buyq = cornerM == lastE
   return buyq

}






    private func stopCameraSession() {

         let gpostfilterRunloop: Bool = rawAppearUnknownData(postsResult:31.0)

      if !gpostfilterRunloop {
          print("ok")
      }

_ = gpostfilterRunloop


       var followingr: String! = String(cString: [102,114,97,99,0], encoding: .utf8)!
      followingr = "\((followingr == (String(cString:[76,0], encoding: .utf8)!) ? followingr.count : followingr.count))"

        captureSession?.stopRunning()
        captureSession = nil
        videoInput = nil
        previewLayer?.removeFromSuperlayer()
        previewLayer = nil
    }

@discardableResult
 func originalProvidingPerformErrorStorageLabel(indexCheck: Bool, restoreBuy: Bool, styleMigrate: Double) -> UILabel! {
    var covern: String! = String(cString: [112,117,98,107,101,121,104,97,115,104,0], encoding: .utf8)!
    var apple7: [Any]! = [true]
   withUnsafeMutablePointer(to: &apple7) { pointer in
          _ = pointer.pointee
   }
   for _ in 0 ..< 2 {
      covern = "\(covern.count * apple7.count)"
   }
   for _ in 0 ..< 2 {
       var placeholderw: [String: Any]! = [String(cString: [97,117,116,104,111,114,0], encoding: .utf8)!:0, String(cString: [115,116,114,101,97,109,102,111,117,114,99,99,0], encoding: .utf8)!:55, String(cString: [117,116,116,111,110,0], encoding: .utf8)!:40]
         placeholderw["\(placeholderw.values.count)"] = placeholderw.count * placeholderw.values.count
       var resetF: String! = String(cString: [110,101,119,115,108,101,116,116,101,114,0], encoding: .utf8)!
       var resolved8: String! = String(cString: [114,101,109,111,116,101,0], encoding: .utf8)!
      for _ in 0 ..< 2 {
         resetF = "\(resetF.count)"
      }
         resolved8 = "\(resetF.count << (Swift.min(resolved8.count, 4)))"
      apple7.append(placeholderw.keys.count << (Swift.min(labs(3), 1)))
   }
     let paragraphSpacing: Bool = false
     var radiusFallback: UIImageView! = UIImageView(image:UIImage(named:String(cString: [115,101,116,105,118,0], encoding: .utf8)!), highlightedImage:UIImage(named:String(cString: [115,101,103,109,97,112,0], encoding: .utf8)!))
     let senderBuilt: [Any]! = [48, 91, 52]
    var widescreenInteractSaltlen:UILabel! = UILabel(frame:CGRect(x: 248, y: 368, width: 0, height: 0))
    widescreenInteractSaltlen.textColor = UIColor(red:1, green:0, blue:0, alpha: 0)
    widescreenInteractSaltlen.textAlignment = .center
    widescreenInteractSaltlen.font = UIFont.systemFont(ofSize:17)
    widescreenInteractSaltlen.text = ""
    widescreenInteractSaltlen.alpha = 0.3;
    widescreenInteractSaltlen.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    widescreenInteractSaltlen.frame = CGRect(x: 196, y: 144, width: 0, height: 0)
    radiusFallback.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    radiusFallback.alpha = 0.3
    radiusFallback.frame = CGRect(x: 181, y: 249, width: 0, height: 0)
    radiusFallback.animationRepeatCount = 0
    radiusFallback.image = UIImage(named:String(cString: [99,104,97,110,103,101,0], encoding: .utf8)!)
    radiusFallback.contentMode = .scaleAspectFit
    

    
    return widescreenInteractSaltlen

}






    private func checkMediaPermissions() {

         let langidGsmdec: UILabel! = originalProvidingPerformErrorStorageLabel(indexCheck:false, restoreBuy:true, styleMigrate:72.0)

      if langidGsmdec != nil {
          let langidGsmdec_tag = langidGsmdec.tag
          self.view.addSubview(langidGsmdec)
      }
      else {
          print("langidGsmdec is nil")      }

_ = langidGsmdec


       var presentingF: String! = String(cString: [115,119,97,98,0], encoding: .utf8)!
    var emptyI: String! = String(cString: [100,111,119,110,103,114,97,100,101,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &emptyI) { pointer in
          _ = pointer.pointee
   }
       var visibility5: String! = String(cString: [115,116,101,112,0], encoding: .utf8)!
       var addressN: Double = 2.0
       _ = addressN
       var horizontalt: String! = String(cString: [109,97,120,101,100,0], encoding: .utf8)!
       var barw: Double = 4.0
       var pending2: Double = 1.0
      if (horizontalt.count - Int(barw > 319663108.0 || barw < -319663108.0 ? 29.0 : barw)) >= 3 && 3 >= (Int(barw > 58942627.0 || barw < -58942627.0 ? 1.0 : barw) - horizontalt.count) {
         horizontalt = "\((Int(barw > 106597848.0 || barw < -106597848.0 ? 82.0 : barw) / 2))"
      }
          var tabi: Float = 0.0
          var author7: Double = 5.0
          var modell: Int = 5
         horizontalt.append("\((Int(author7 > 278005300.0 || author7 < -278005300.0 ? 96.0 : author7) << (Swift.min(5, labs(1)))))")
         tabi += (Float(Int(author7 > 309556963.0 || author7 < -309556963.0 ? 69.0 : author7) % 3))
         modell >>= Swift.min(labs((horizontalt.count ^ Int(barw > 47812703.0 || barw < -47812703.0 ? 60.0 : barw))), 1)
      while (visibility5 != horizontalt) {
         horizontalt = "\((Int(pending2 > 350328630.0 || pending2 < -350328630.0 ? 80.0 : pending2)))"
         break
      }
          var local_9zV: [String: Any]! = [String(cString: [101,97,114,108,105,101,114,0], encoding: .utf8)!:String(cString: [112,108,97,110,101,0], encoding: .utf8)!, String(cString: [112,111,115,116,114,111,116,97,116,101,0], encoding: .utf8)!:String(cString: [109,108,112,100,115,112,0], encoding: .utf8)!, String(cString: [99,108,97,115,115,110,97,109,101,0], encoding: .utf8)!:String(cString: [109,101,109,117,116,105,108,0], encoding: .utf8)!]
         withUnsafeMutablePointer(to: &local_9zV) { pointer in
                _ = pointer.pointee
         }
          var info4: [Any]! = [97, 6, 24]
         withUnsafeMutablePointer(to: &info4) { pointer in
    
         }
          var builtl: [String: Any]! = [String(cString: [97,116,116,114,105,98,117,116,101,100,0], encoding: .utf8)!:22, String(cString: [118,99,114,101,97,116,101,0], encoding: .utf8)!:91, String(cString: [112,108,97,116,102,111,114,109,0], encoding: .utf8)!:26]
         pending2 -= Double(local_9zV.count / 1)
         info4.append(3 ^ horizontalt.count)
         builtl["\(addressN)"] = (local_9zV.values.count & Int(addressN > 286255360.0 || addressN < -286255360.0 ? 40.0 : addressN))
         horizontalt = "\((Int(pending2 > 131555303.0 || pending2 < -131555303.0 ? 52.0 : pending2)))"
       var home7: String! = String(cString: [119,111,114,100,115,0], encoding: .utf8)!
       var enableda: String! = String(cString: [115,99,101,110,97,114,105,111,0], encoding: .utf8)!
       _ = enableda
       var revise5: String! = String(cString: [112,108,97,110,97,114,116,111,121,117,121,0], encoding: .utf8)!
       var indicatorx: String! = String(cString: [115,104,97,114,101,0], encoding: .utf8)!
         addressN -= Double(enableda.count ^ 3)
         home7 = "\(horizontalt.count)"
         revise5.append("\((enableda == (String(cString:[104,0], encoding: .utf8)!) ? Int(pending2 > 193516820.0 || pending2 < -193516820.0 ? 82.0 : pending2) : enableda.count))")
         indicatorx = "\((Int(pending2 > 116109867.0 || pending2 < -116109867.0 ? 6.0 : pending2) % 3))"
      presentingF.append("\(1)")

        ensurePermission(for: .video) { [weak self] cameraGranted in
            guard let self else { return }
            self.ensurePermission(for: .audio) { micGranted in
   while (presentingF == String(cString:[68,0], encoding: .utf8)!) {
       var questionk: String! = String(cString: [105,110,115,116,97,108,108,105,110,103,0], encoding: .utf8)!
       var setupO: String! = String(cString: [112,114,101,115,101,108,101,99,116,0], encoding: .utf8)!
         setupO = "\((setupO == (String(cString:[78,0], encoding: .utf8)!) ? setupO.count : questionk.count))"
         questionk = "\(setupO.count >> (Swift.min(questionk.count, 5)))"
         setupO.append("\(setupO.count)")
      while (setupO.count < 2 && 2 < questionk.count) {
         setupO.append("\(questionk.count * setupO.count)")
         break
      }
      if !questionk.hasSuffix("\(setupO.count)") {
         questionk = "\(setupO.count)"
      }
      if setupO.count >= 4 || questionk != String(cString:[81,0], encoding: .utf8)! {
         setupO = "\(setupO.count)"
      }
      emptyI = "\(setupO.count)"
      break
   }
                if cameraGranted && micGranted {
                    self.startCameraSession()
                } else {
                    self.showPermissionAlert()
                }
            }
        }
    }


    private func cameraDevice(for position: AVCaptureDevice.Position) -> AVCaptureDevice? {
       var storedG: Int = 3
    _ = storedG
   repeat {
      storedG &= 3
      if 4428622 == storedG {
         break
      }
   } while (4428622 == storedG) && (3 == storedG)

return         AVCaptureDevice.default(.builtInWideAngleCamera, for: .video, position: position)
    }


    private func switchCamera() {
       var merge9: String! = String(cString: [115,116,97,98,108,101,0], encoding: .utf8)!
    _ = merge9
    var publishg: [Any]! = [true]
   while (4 <= (3 | merge9.count) || (3 | merge9.count) <= 2) {
      publishg = [merge9.count]
      break
   }

        guard isCameraEnabled, let session = captureSession, let currentInput = videoInput else { return }

   while ((4 & publishg.count) >= 1 && (publishg.count & merge9.count) >= 4) {
      merge9.append("\(1)")
      break
   }
        currentCameraPosition = currentCameraPosition == .front ? .back : .front
        guard
            let overlay = cameraDevice(for: currentCameraPosition),
            let description_hx = try? AVCaptureDeviceInput(device: overlay)
        else {
            return
        }

        session.beginConfiguration()
        session.removeInput(currentInput)
        if session.canAddInput(description_hx) {
            session.addInput(description_hx)
            videoInput = description_hx
        } else {
            session.addInput(currentInput)
        }
        session.commitConfiguration()
    }


    @objc private func clickCameraButton() {
       var blueT: [Any]! = [96, 50, 9]
      blueT.append(blueT.count % (Swift.max(blueT.count, 5)))

        isCameraEnabled.toggle()
        if isCameraEnabled {
            startCameraSession()
        } else if let session = captureSession, session.isRunning {
            DispatchQueue.global(qos: .userInitiated).async {
                session.stopRunning()
            }
        }
        updateControlButtons()
    }

    private let previewView: UIView = {
       var welcomeT: String! = String(cString: [100,105,115,112,101,110,115,101,114,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &welcomeT) { pointer in
    
   }
    var rnewss: String! = String(cString: [116,101,115,116,0], encoding: .utf8)!
   repeat {
      welcomeT.append("\(welcomeT.count * rnewss.count)")
      if (String(cString:[108,116,108,57,0], encoding: .utf8)!) == welcomeT {
         break
      }
   } while ((String(cString:[108,116,108,57,0], encoding: .utf8)!) == welcomeT) && (!welcomeT.contains("\(rnewss.count)"))

        let view = UIView()
      rnewss = "\(rnewss.count - welcomeT.count)"
        view.backgroundColor = .black
        return view
    }()

    private let backButton: UIButton = {
       var stackV: Bool = false
    _ = stackV
    var result8: Int = 5
   if stackV {
      result8 -= result8
   }

        let button = UIButton(type: .custom)
   repeat {
      stackV = !stackV
      if stackV ? !stackV : stackV {
         break
      }
   } while (stackV ? !stackV : stackV) && ((1 | result8) < 4)
        button.setImage(UIImage(named: "common_back"), for: .normal)
        return button
    }()

    private let peerContainerView: UIView = {
       var showL: Float = 5.0
    _ = showL
   while (5.47 >= (Double(showL / (Swift.max(Float(3), 7))))) {
      showL += (Float(Int(showL > 118603849.0 || showL < -118603849.0 ? 44.0 : showL) | 3))
      break
   }

        let view = UIView()
        view.backgroundColor = UIColor(hex: "#E8E8E8")
        view.layer.cornerRadius = 20
        view.layer.masksToBounds = true
        return view
    }()

    private let peerAvatarView: UIImageView = {
       var bundled: String! = String(cString: [112,101,114,109,105,115,115,105,111,110,115,0], encoding: .utf8)!
   if 3 == bundled.count {
       var attributes1: Int = 2
       var presentingp: Double = 0.0
         presentingp -= Double(attributes1)
          var configurationb: String! = String(cString: [112,108,116,101,0], encoding: .utf8)!
         presentingp /= Swift.max(3, Double(3))
         configurationb = "\((Int(presentingp > 178511110.0 || presentingp < -178511110.0 ? 87.0 : presentingp) | configurationb.count))"
      if 4 >= (1 << (Swift.min(4, labs(attributes1)))) {
         presentingp -= (Double(Int(presentingp > 90182131.0 || presentingp < -90182131.0 ? 79.0 : presentingp) - 1))
      }
      for _ in 0 ..< 1 {
         presentingp += (Double(Int(presentingp > 364099600.0 || presentingp < -364099600.0 ? 54.0 : presentingp) << (Swift.min(5, labs(attributes1)))))
      }
         attributes1 %= Swift.max(1, 1)
          var toolz: [String: Any]! = [String(cString: [111,118,101,114,102,108,111,119,0], encoding: .utf8)!:[String(cString: [97,99,99,101,115,115,105,110,103,0], encoding: .utf8)!:85, String(cString: [118,105,109,101,111,0], encoding: .utf8)!:24, String(cString: [100,105,115,99,114,101,116,101,0], encoding: .utf8)!:69]]
          var with_6sa: [String: Any]! = [String(cString: [101,120,116,101,110,115,105,111,110,0], encoding: .utf8)!:[75, 56, 99]]
          var c_viewm: String! = String(cString: [109,97,116,114,105,120,101,110,99,0], encoding: .utf8)!
         presentingp /= Swift.max(2, (Double(3 - Int(presentingp > 87633764.0 || presentingp < -87633764.0 ? 85.0 : presentingp))))
         toolz["\(attributes1)"] = ((String(cString:[56,0], encoding: .utf8)!) == c_viewm ? c_viewm.count : attributes1)
         with_6sa["\(attributes1)"] = (Int(presentingp > 81075928.0 || presentingp < -81075928.0 ? 56.0 : presentingp))
      bundled.append("\((bundled.count | Int(presentingp > 67396040.0 || presentingp < -67396040.0 ? 31.0 : presentingp)))")
   }

        let view = UIImageView()
        view.contentMode = .scaleAspectFill
        view.clipsToBounds = true
        return view
    }()

    private let loadingIndicator: UIActivityIndicatorView = {
       var attachment6: Bool = false
    _ = attachment6
      attachment6 = !attachment6

        let indicator = UIActivityIndicatorView(style: .large)
        indicator.color = .white
        indicator.hidesWhenStopped = false
        return indicator
    }()

    private let bottomPanelView: UIView = {
       var namesO: Float = 0.0
      namesO += Float(1)

        let view = UIView()
        view.backgroundColor = .white
        view.layer.cornerRadius = 28
        view.layer.maskedCorners = [.layerMinXMinYCorner, .layerMaxXMinYCorner]
        view.layer.masksToBounds = true
        return view
    }()

    private let controlStackView: UIStackView = {
       var mediaz: Double = 1.0
   if mediaz == 2.50 {
      mediaz += Double(3)
   }

        let stack = UIStackView()
        stack.axis = .horizontal
        stack.distribution = .equalSpacing
        stack.alignment = .center
        return stack
    }()

    private let reverseButton: UIButton = {
       var configurationi: [Any]! = [String(cString: [108,97,117,110,99,104,101,115,0], encoding: .utf8)!, String(cString: [111,118,101,114,114,105,100,105,110,103,0], encoding: .utf8)!, String(cString: [101,120,104,97,117,115,116,101,100,0], encoding: .utf8)!]
   for _ in 0 ..< 1 {
      configurationi.append(configurationi.count >> (Swift.min(labs(1), 3)))
   }

        let button = UIButton(type: .custom)
        button.setImage(UIImage(named: "room_reverse"), for: .normal)
        return button
    }()

    private let cameraButton: UIButton = {
       var storeF: String! = String(cString: [108,97,98,101,108,0], encoding: .utf8)!
   repeat {
       var showk: [String: Any]! = [String(cString: [109,117,108,109,111,100,0], encoding: .utf8)!:62, String(cString: [105,110,116,101,114,114,97,99,116,105,118,101,0], encoding: .utf8)!:1]
         showk = ["\(showk.values.count)": showk.keys.count * 3]
      while (4 < (showk.keys.count + showk.count) && 4 < (showk.count + showk.keys.count)) {
         showk = ["\(showk.values.count)": showk.values.count & showk.count]
         break
      }
         showk = ["\(showk.values.count)": 3]
      storeF = "\(showk.keys.count & 2)"
      if storeF == (String(cString:[120,57,56,54,102,106,120,0], encoding: .utf8)!) {
         break
      }
   } while (storeF == (String(cString:[120,57,56,54,102,106,120,0], encoding: .utf8)!)) && (storeF != storeF)

        let button = UIButton(type: .custom)
        button.setImage(UIImage(named: "room_camera"), for: .normal)
        return button
    }()

    private let micButton: UIButton = {
       var agreementu: String! = String(cString: [118,99,120,112,114,111,106,0], encoding: .utf8)!
      agreementu = "\(((String(cString:[68,0], encoding: .utf8)!) == agreementu ? agreementu.count : agreementu.count))"

        let button = UIButton(type: .custom)
        button.setImage(UIImage(named: "room_mic"), for: .normal)
        return button
    }()

    private let hangUpButton: UIButton = {
       var directoryD: Float = 3.0
    _ = directoryD
    var w_viewt: Float = 3.0
      directoryD -= (Float(Int(directoryD > 73742902.0 || directoryD < -73742902.0 ? 2.0 : directoryD) >> (Swift.min(5, labs(1)))))

        let button = UIButton(type: .custom)
   if (directoryD + w_viewt) <= 3.29 || 3.29 <= (directoryD + w_viewt) {
      w_viewt += (Float(Int(directoryD > 375565172.0 || directoryD < -375565172.0 ? 4.0 : directoryD)))
   }
        button.setImage(UIImage(named: "room_off"), for: .normal)
        return button
    }()
}
